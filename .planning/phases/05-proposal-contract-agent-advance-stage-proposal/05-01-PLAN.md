---
phase: 05-proposal-contract-agent-advance-stage-proposal
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/models/engagement_record.py
  - backend/tools/check_scope_clarity.py
  - backend/tools/draft_proposal.py
  - backend/tools/draft_contract.py
  - backend/agents/proposal_runner.py
  - backend/api.py
  - backend/tests/test_proposal_runner.py
  - backend/tests/test_advance_endpoint.py
  - backend/tests/test_advance_bedrock_failfast.py
autonomous: true
requirements: [PROP-01, PROP-02, PROP-03, PROP-04]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Advancing a clear-scope, apply-verdict engagement to stage=proposal returns a phased-scope proposal, a contract (SOW with enumerable deliverables + milestones + payment terms), and a structured payment schedule (SC1)."
    - "Advancing a deliberately ambiguous fixture (missing budget, timeline, or deliverables) to stage=proposal returns needs_human_input=true with a specific question, not a guessed proposal/contract, and raises no structured-output exception (SC2)."
    - "The Engagement Record's proposal and contract slices are populated only via FastAPI's verbatim merge of the specialist's typed output (SC4/D-05)."
    - "POST /engagements/{id}/advance returns 404 for an unknown id and 409 for a non-apply/untriaged engagement (D-05)."
    - "The deterministic path (PROPOSAL_BACKEND unset) yields the SC1 and SC2 outcomes offline with placeholder AWS credentials (D-02/D-07)."
  artifacts:
    - backend/models/engagement_record.py
    - backend/tools/check_scope_clarity.py
    - backend/tools/draft_proposal.py
    - backend/tools/draft_contract.py
    - backend/agents/proposal_runner.py
    - backend/api.py
    - backend/tests/test_proposal_runner.py
    - backend/tests/test_advance_endpoint.py
    - backend/tests/test_advance_bedrock_failfast.py
  key_links:
    - "api.py advance() -> get_proposal_runner (DI seam) -> _deterministic_proposal_runner -> check_scope_clarity / draft_proposal / draft_contract"
    - "advance() merges ProposalContractResult verbatim into record.proposal (+ record.contract on happy path) then store.save(record)"
    - "ProposalContractResult.model_validator(mode='after') enforces D-01 mutual exclusivity (happy XOR escalation)"
  prohibitions:
    - statement: "No /advance response contains both a fully populated contract and needs_human_input=true (SC3)."
      status: enforced
      verification: "cd backend && python3 -m pytest tests/test_proposal_runner.py -k exclusivity -q — ProposalContractResult.model_validator raises when both branches populated"
    - statement: "No module under backend/agents or backend/tools imports the store (REC-03/D-05)."
      status: enforced
      verification: "cd backend && python3 -m pytest tests/test_single_writer.py -q"
    - statement: "No /advance error response leaks a raw AWS Message or credential literal."
      status: enforced
      verification: "cd backend && python3 -m pytest tests/test_advance_bedrock_failfast.py -q — asserts the secret Message text is absent from the 503 detail"
---

<objective>
Build the Stage 2 Proposal-Contract capability end-to-end on the DETERMINISTIC path: enrich the
Engagement Record schema with a typed payment schedule and a mutually-exclusive result model,
add the three PRD §7.2 dual-use tools, add the `ProposalRunner` DI seam, and expose
`POST /engagements/{id}/advance?stage=proposal` as the sole writer of the proposal/contract
slices. This is the demo-critical, fully-offline path (PROPOSAL_BACKEND unset → deterministic),
faithfully mirroring the Phase-3 `/capture` → TriageRunner → verbatim-merge → store precedent.

Purpose: deliver PROP-01..04 as machine-checkable, repeatable behavior with placeholder AWS
credentials, satisfying SC1 (clear-scope draft), SC2 (ambiguous → targeted question, no crash),
SC3 (mutual exclusivity), and SC4 (FastAPI-only verbatim merge).
Output: 6 production files (3 new tools, 1 new runner, 2 modified: model + api) and 3 new test
files. The live two-agent Bedrock path (D-04/D-07(f)) is Plan 05-02.

Phase dependency: this branch is stacked on gsd/phase-03 — the Engagement Record, store,
Supervisor, Gig Triage specialist, `/capture`, and `GET /engagements/{id}` are already present.
</objective>

<execution_context>
@/home/user/Freelance-Autopilot/.claude/gsd-core/workflows/execute-plan.md
@/home/user/Freelance-Autopilot/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-proposal-contract-agent-advance-stage-proposal/05-CONTEXT.md
@.planning/phases/05-proposal-contract-agent-advance-stage-proposal/05-RESEARCH.md
@.planning/phases/05-proposal-contract-agent-advance-stage-proposal/05-PATTERNS.md

# Phase-3 foundation this plan mirrors (read the exact analogs before writing):
@backend/api.py
@backend/models/engagement_record.py
@backend/agents/triage_runner.py
@backend/tools/placeholder_triage.py
@backend/store/engagement_store.py
@backend/tests/test_capture_endpoint.py
@backend/tests/test_capture_bedrock_failfast.py
@backend/tests/test_triage_runner.py
@backend/tests/conftest.py
</context>

<artifacts_produced>
Symbols and files this plan creates or modifies:
- `PaymentMilestone` (new Pydantic model — label:str, amount:float, due_marker:str) in `backend/models/engagement_record.py`
- `ProposalContractResult` (new mutually-exclusive result model + `model_validator`) in `backend/models/engagement_record.py`
- `ContractSlice.payment_schedule` retyped `list[dict]` → `list[PaymentMilestone]` (D-06) in `backend/models/engagement_record.py`
- `check_scope_clarity` (new `@tool`) in `backend/tools/check_scope_clarity.py`
- `draft_proposal` (new `@tool`) in `backend/tools/draft_proposal.py`
- `draft_contract` (new `@tool`) in `backend/tools/draft_contract.py`
- `ProposalRunner` (Protocol), `_deterministic_proposal_runner`, `_supervisor_proposal_runner`, `get_proposal_runner`, `PROPOSAL_BACKEND` env read in `backend/agents/proposal_runner.py`
- `advance` route (`POST /engagements/{engagement_id}/advance`) in `backend/api.py`
- Test files: `backend/tests/test_proposal_runner.py`, `backend/tests/test_advance_endpoint.py`, `backend/tests/test_advance_bedrock_failfast.py`
</artifacts_produced>

<tasks>

<task type="tracer">
  <name>Task 1: TRACER — clear-scope /advance -> deterministic runner -> verbatim merge -> save, end-to-end</name>
  <read_first>
    - backend/models/engagement_record.py (existing ProposalSlice/ContractSlice stubs, lines 31-39; JobSlice lines 18-22; style: Optional[...] = None, Field(default_factory=list))
    - backend/agents/triage_runner.py (the TriageRunner seam to mirror EXACTLY — Protocol, _deterministic_*, _supervisor_* lazy-import, get_*_runner env read)
    - backend/tools/placeholder_triage.py (dual-use @tool pattern; keyword-scan structure; store-import prohibition docstring)
    - backend/api.py (capture handler lines 97-121, get_engagement 124-132, map_bedrock_error 62-94, get_store, Annotated/Depends imports)
    - .planning/phases/05-proposal-contract-agent-advance-stage-proposal/05-RESEARCH.md (Pattern 1 schema, Pattern 2 composition, Pattern 4 endpoint)
  </read_first>
  <files>backend/models/engagement_record.py, backend/tools/check_scope_clarity.py, backend/tools/draft_proposal.py, backend/tools/draft_contract.py, backend/agents/proposal_runner.py, backend/api.py, backend/tests/test_advance_endpoint.py</files>
  <behavior>
    - Given a captured engagement whose triage.verdict == "apply" and whose job has a budget and a description containing timeline + deliverable signals, POST /engagements/{id}/advance?stage=proposal returns 200, and the persisted record has record.proposal.text populated, record.proposal.needs_human_input == false, record.contract.text populated, and record.contract.payment_schedule is a non-empty list of typed milestones (label/amount/due_marker).
    - The returned body equals what GET /engagements/{id} returns afterward (verbatim persistence).
  </behavior>
  <action>
Enrich the model, write the three deterministic tools, write the runner seam, and add the endpoint — wiring ONE happy path (clear-scope apply engagement) through every layer.

1) `backend/models/engagement_record.py` (per D-01, D-06): import `model_validator` from pydantic. Add `PaymentMilestone(BaseModel)` with fields `label: str`, `amount: float`, `due_marker: str` (freeform symbolic marker such as on_signing / on_delivery / net_15 — not a calendar date). Retype `ContractSlice.payment_schedule` from `list[dict]` to `list[PaymentMilestone]` (keep `text: Optional[str] = None` and `Field(default_factory=list)`). Add `ProposalContractResult(BaseModel)` with `needs_human_input: bool = False`, `question: Optional[str] = None`, `proposal_text: Optional[str] = None`, `contract_text: Optional[str] = None`, `payment_schedule: list[PaymentMilestone] = Field(default_factory=list)`. Add a `@model_validator(mode="after")` that raises `ValueError` when: needs_human_input is true AND any happy field (proposal_text/contract_text/payment_schedule) is populated; OR needs_human_input is true AND question is empty; OR needs_human_input is false AND not all of (proposal_text AND contract_text AND non-empty payment_schedule) are present. This is the structural SC3 guard — construct the model, never a bare dict (RESEARCH Pitfall C).

2) `backend/tools/check_scope_clarity.py` (D-03): `from strands import tool`. Module docstring states it must NOT import the store (single-writer guard, mirror placeholder_triage.py docstring). Define module-level keyword sets: TIMELINE_MARKERS = {week, weeks, month, months, deadline, asap, "by ", days} and DELIVERABLE_MARKERS = {deliverable, milestone, pages, wireframe, revisions, phase}. Define `@tool def check_scope_clarity(budget: float | None, description: str) -> dict` — a deterministic gate, no LLM: lowercase the description; collect a missing list — append "budget" if budget is None, "timeline" if no TIMELINE_MARKERS substring matches, "deliverables" if no DELIVERABLE_MARKERS substring matches. If missing is non-empty return {"clear": False, "question": a specific question naming the joined missing fields}; else return {"clear": True, "question": None}. Same input always yields the same output.

3) `backend/tools/draft_proposal.py` (PROP-01): `@tool def draft_proposal(title: str, description: str, budget: float) -> dict` — deterministic phased-scope template returning {"proposal_text": ...} with three named phases (Discovery & scoping, Core delivery, Revisions & handoff) and the budget rendered. No LLM, no store import.

4) `backend/tools/draft_contract.py` (PROP-02/PROP-03): `@tool def draft_contract(title: str, description: str, proposal_text: str, budget: float) -> dict` — deterministic SOW template returning {"contract_text": ..., "payment_schedule": [...]}. contract_text enumerates deliverables (three named deliverables) + references milestone-based payment. payment_schedule is a list of three dicts each with label/amount/due_marker; amount is the ABSOLUTE dollar figure computed from budget (0.3, 0.5, 0.2 split, rounded to 2 dp) — budget is guaranteed present on the happy path because check_scope_clarity escalates when budget is None. Keep the amount unit consistent (absolute dollars, per RESEARCH A2). No store import.

5) `backend/agents/proposal_runner.py` (D-02): mirror triage_runner.py EXACTLY. Module docstring restates the single-writer prohibition (must NOT import the store). `from __future__ import annotations`; `import os`; `from typing import Protocol`; import `JobSlice, ProposalContractResult` from models.engagement_record and the three tool functions from tools.*. Define `class ProposalRunner(Protocol): def __call__(self, job: JobSlice) -> ProposalContractResult: ...`. Define `_deterministic_proposal_runner(job)`: call `check_scope_clarity(job.budget, job.description)` as a plain function; if not clear, return `ProposalContractResult(needs_human_input=True, question=clarity["question"])`; else call `draft_proposal(...)` then `draft_contract(..., budget=job.budget)` and return `ProposalContractResult(proposal_text=..., contract_text=..., payment_schedule=contract["payment_schedule"])` — Pydantic coerces the list-of-dicts into list[PaymentMilestone]. Define `_supervisor_proposal_runner(job)` with a lazy `from agents.supervisor import build_proposal_supervisor, extract_proposal_result` INSIDE the function body (these are authored in Plan 05-02; this path is manual/live-only and is never invoked by an automated test) — construct the supervisor, invoke it with a prompt carrying `job.model_dump_json()`, and return `extract_proposal_result(supervisor.messages)`. Define `get_proposal_runner()` reading `os.environ.get("PROPOSAL_BACKEND", "placeholder")`, returning `_supervisor_proposal_runner` when it equals "supervisor" else `_deterministic_proposal_runner`.

6) `backend/api.py` (D-05): add imports for `ProposalRunner, get_proposal_runner` from agents.proposal_runner and for `ProposalSlice, ContractSlice, ProposalContractResult` from models.engagement_record (extend the existing import line). Add `advance` handler: `@app.post("/engagements/{engagement_id}/advance", response_model=EngagementRecord)` with params `engagement_id: UUID`, `stage: str`, `store: Annotated[EngagementStore, Depends(get_store)]`, `proposal_runner: Annotated[ProposalRunner, Depends(get_proposal_runner)]`. Body: load `record = store.get(engagement_id)`; if None raise HTTPException 404 "Engagement not found". If `stage != "proposal"` raise HTTPException 400 (unsupported stage — Phase 6 adds an `elif stage == "ops"` branch here without rewriting the guard/merge). If `record.triage is None or record.triage.verdict != "apply"` raise HTTPException 409 "engagement is not apply-triaged; cannot draft a proposal". Then `try: result = proposal_runner(record.job)` with the SAME except ladder as capture (`except HTTPException: raise` then `except Exception as exc: mapped = map_bedrock_error(exc); raise HTTPException(status_code=503, detail=str(mapped)) from mapped`). Merge VERBATIM: `record.proposal = ProposalSlice(text=result.proposal_text, needs_human_input=result.needs_human_input, question=result.question)`; `if not result.needs_human_input: record.contract = ContractSlice(text=result.contract_text, payment_schedule=result.payment_schedule)`. Then `store.save(record)` (NOT create — mutating an existing record) and `return record`. Reuse `map_bedrock_error` unmodified — do not fork it.

7) `backend/tests/test_advance_endpoint.py` — the tracer's end-to-end proof only (guard/merge depth is Task 3). Use the existing `client` fixture (conftest.py, unchanged). Write ONE happy-path test: POST /capture a clear-scope apply job (title, a description containing a timeline word and a deliverable word, budget=2000.0) → capture returns apply; then POST /engagements/{id}/advance?stage=proposal → assert 200, body proposal.text truthy, body proposal.needs_human_input is false, body contract.text truthy, body contract.payment_schedule is a non-empty list whose first item has keys label/amount/due_marker; then GET /engagements/{id} and assert its proposal/contract equal the advance response (verbatim persistence).

Do NOT place fenced code blocks in production comments that a later negative-grep gate would scan. Keep the deterministic path the DEFAULT so the suite passes offline.
  </action>
  <verify>
    <automated>cd backend && python3 -m pytest tests/test_advance_endpoint.py -q</automated>
    <fails_when>non-zero exit, or "0 passed" / "no tests ran" in the pytest summary line, or any failed/error count > 0</fails_when>
    <automated>cd backend && python3 -m pytest tests/test_single_writer.py -q</automated>
    <fails_when>non-zero exit, or the REC-03 violation assertion fails (a new agents/tools module imports the store)</fails_when>
    <automated>cd backend && python3 -m pytest</automated>
    <fails_when>non-zero exit, or fewer than the Phase-3 baseline (37) + the new tests pass, or any failed/error count > 0</fails_when>
  </verify>
  <acceptance_criteria>
    - `POST /engagements/{id}/advance?stage=proposal` on a clear-scope apply engagement returns 200 with a populated proposal, a populated contract, and a non-empty typed payment_schedule.
    - The advance response round-trips identically through `GET /engagements/{id}` (verbatim persistence, SC4 happy path).
    - `ProposalContractResult`, `PaymentMilestone` exist; `ContractSlice.payment_schedule` is `list[PaymentMilestone]`.
    - `get_proposal_runner` defaults to `_deterministic_proposal_runner` when PROPOSAL_BACKEND is unset.
    - `test_single_writer.py` still passes (no new agents/tools module imports the store).
    - The full existing suite remains green (no Phase-3 regressions).
  </acceptance_criteria>
  <done>The clear-scope happy path works end-to-end offline and is committed; the full suite is green.</done>
  <reversibility rating="costly">D-01 result schema and D-06 typed payment schedule are the persisted/merged contract shape; changing them later touches the model, the merge, and every proposal-stage test together (no production records exist yet, so this is a costly refactor, not one-way).</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit-depth — tools + deterministic runner + mutual-exclusivity (PROP-01..04, SC2, SC3)</name>
  <read_first>
    - backend/tests/test_triage_runner.py (the analog: pure-function unit tests, env-selection tests via monkeypatch)
    - backend/tools/check_scope_clarity.py, backend/tools/draft_proposal.py, backend/tools/draft_contract.py (bodies written in Task 1)
    - backend/agents/proposal_runner.py (the seam written in Task 1)
    - backend/models/engagement_record.py (ProposalContractResult validator written in Task 1)
    - .planning/phases/05-proposal-contract-agent-advance-stage-proposal/05-RESEARCH.md (Pitfall B: keyword lists and fixtures must agree; Pitfall C)
  </read_first>
  <files>backend/tests/test_proposal_runner.py</files>
  <behavior>
    - check_scope_clarity flags exactly the missing fields: budget=None → "budget" cited; a description with no timeline word → "timeline" cited; no deliverable word → "deliverables" cited; a clear description with budget → clear=True.
    - _deterministic_proposal_runner on a clear-scope job returns a ProposalContractResult with proposal_text, contract_text, and a non-empty payment_schedule and needs_human_input=False.
    - _deterministic_proposal_runner on an ambiguous job (missing budget/timeline/deliverables) returns needs_human_input=True with a non-empty question, no populated contract, and raises NO exception.
    - Constructing ProposalContractResult with BOTH needs_human_input=True and a populated contract_text raises ValueError; constructing with needs_human_input=False and a missing happy field raises ValueError.
    - get_proposal_runner selects _supervisor_proposal_runner when PROPOSAL_BACKEND=supervisor and _deterministic_proposal_runner by default.
  </behavior>
  <action>
Author `backend/tests/test_proposal_runner.py` mirroring test_triage_runner.py's shape (plain-function unit tests, no TestClient). Import the three tools, the runner symbols (`_deterministic_proposal_runner`, `_supervisor_proposal_runner`, `get_proposal_runner`), and `JobSlice`, `ProposalContractResult`, `PaymentMilestone`.

Write these test groups (use `-k` selectable names — include the words draft_proposal, draft_contract, payment_schedule, ambiguous, exclusivity in the relevant test names so RESEARCH's Test Map `-k` filters resolve):
- draft_proposal (PROP-01): asserts phased-scope text mentions the three phases and the budget; deterministic (same input twice → equal output).
- draft_contract (PROP-02): asserts SOW text enumerates deliverables; deterministic.
- payment_schedule (PROP-03): asserts draft_contract returns a payment_schedule whose items each have label/amount/due_marker, amounts are absolute dollars summing to the budget, and that ProposalContractResult coerces them into list[PaymentMilestone] instances.
- scope-clarity gate (PROP-04): parametrize the missing-field cases — budget=None cites budget; a description lacking timeline words cites timeline; lacking deliverable words cites deliverables; a clear description+budget → clear=True. Assert the EXACT missing set for the ambiguous fixture (Pitfall B — not just "escalates").
- ambiguous runner path (SC2): _deterministic_proposal_runner on an ambiguous JobSlice returns needs_human_input=True + non-empty question, contract_text is None, and no exception is raised (wrap in a plain call, assert on the result).
- exclusivity (SC3): assert ProposalContractResult(needs_human_input=True, question="q", contract_text="x") raises ValueError; assert ProposalContractResult(needs_human_input=False) (missing happy fields) raises ValueError; assert a valid happy-path construction and a valid escalation construction both succeed.
- env selection: monkeypatch PROPOSAL_BACKEND=supervisor → get_proposal_runner() is _supervisor_proposal_runner; delenv → is _deterministic_proposal_runner (mirror test_triage_runner.py exactly).

Author the ambiguous and clear fixtures' description strings in THIS task alongside the assertions so wording and keyword lists agree (Pitfall B).
  </action>
  <verify>
    <automated>cd backend && python3 -m pytest tests/test_proposal_runner.py -q</automated>
    <fails_when>non-zero exit, or "0 passed" / "no tests ran" in the summary line, or any failed/error count > 0</fails_when>
    <automated>cd backend && python3 -m pytest tests/test_proposal_runner.py -k "exclusivity or ambiguous" -q</automated>
    <fails_when>non-zero exit, or the exclusivity/ambiguous selection matches 0 tests, or any failure</fails_when>
  </verify>
  <acceptance_criteria>
    - PROP-01/02/03 tool behaviors are asserted deterministically (repeat-call stability + structural checks).
    - PROP-04/SC2: the ambiguous fixture escalates with a specific question citing the exact missing fields, with no exception.
    - SC3: both-populated and neither-populated ProposalContractResult constructions raise ValueError; valid happy and valid escalation constructions succeed.
    - Payment schedule items coerce into `PaymentMilestone` and amounts are absolute dollars.
    - Env-selection tests pass for both PROPOSAL_BACKEND=supervisor and default.
  </acceptance_criteria>
  <done>All deterministic tool/runner/validator behaviors for PROP-01..04, SC2, SC3 are covered and green.</done>
</task>

<task type="auto">
  <name>Task 3: Endpoint-contract depth — guards, verbatim merge (SC4), 503 fail-fast (D-07g)</name>
  <read_first>
    - backend/tests/test_capture_endpoint.py (round-trip + malformed-payload shape)
    - backend/tests/test_engagements_endpoint.py (404-for-unknown-id shape)
    - backend/tests/test_capture_bedrock_failfast.py (the EXACT parametrized override-and-raise pattern to mirror; secret-not-leaked assertions)
    - backend/api.py (the advance handler written in Task 1; map_bedrock_error taxonomy 62-94)
    - backend/tests/conftest.py (client / file_store fixtures — unchanged, no new fixture needed)
  </read_first>
  <files>backend/tests/test_advance_endpoint.py, backend/tests/test_advance_bedrock_failfast.py</files>
  <behavior>
    - /advance returns 404 for an unknown engagement_id.
    - /advance returns 409 when the engagement has no triage, and 409 when triage.verdict != "apply".
    - /advance returns 400 for an unsupported stage value (e.g. "ops").
    - An ambiguous-scope apply engagement advanced to proposal persists proposal.needs_human_input=true + question and leaves contract null (SC2/SC3 at the endpoint), round-tripping via GET.
    - When the injected runner raises (any of the Phase-3 exception types), /advance returns 503 (never a raw 500) and the detail never contains the raw AWS Message / secret.
  </behavior>
  <action>
Extend `backend/tests/test_advance_endpoint.py` (created in Task 1) with the guard + escalation + verbatim-merge cases, and create `backend/tests/test_advance_bedrock_failfast.py` mirroring test_capture_bedrock_failfast.py.

In test_advance_endpoint.py add:
- guard 404: POST /engagements/{random uuid4}/advance?stage=proposal → 404.
- guard 409 (no triage): construct/persist an engagement with triage=None is not reachable via /capture (capture always triages), so use the `file_store` fixture to save an EngagementRecord with job only (no triage) via the store directly in the test setup, then advance → 409. (The test module MAY import the store — the single-writer rule constrains agents/ and tools/, not tests.)
- guard 409 (skip verdict): capture a job that the placeholder triage SKIPS (budget below the 100.0 floor, e.g. budget=10.0, or a red-flag keyword) → verdict skip; advance → 409.
- guard 400 (unsupported stage): capture an apply job, advance with stage="ops" → 400.
- SC2/SC3 at endpoint: capture an apply job whose description is deliberately ambiguous (missing timeline AND deliverable words) with a budget present so it still triages apply but check_scope_clarity escalates on timeline/deliverables; advance → 200 with proposal.needs_human_input true, a question, and contract null; GET round-trips the same.
- SC4 verbatim: reuse the Task-1 happy path assertion style but additionally assert the persisted payment_schedule items equal what advance returned (verbatim, no re-authoring).

In test_advance_bedrock_failfast.py: copy the `_raising_runner` helper and the parametrize list from test_capture_bedrock_failfast.py (ModelThrottledException, ContextWindowOverflowException, RuntimeError, plus the ClientError-with-secret-Message case and NoCredentialsError case). For each: first capture an apply engagement (budget=500 clean job → apply), THEN set `app.dependency_overrides[get_proposal_runner] = lambda: _raising_runner(exc)` inside try/finally (del on teardown), POST /advance?stage=proposal, assert status 503, assert the secret Message text ("top secret detail") is NOT in the detail, assert the ClientError Code IS present, and assert "credentials" appears for NoCredentialsError. Import `get_proposal_runner` from api (re-exported via the proposal_runner import) or from agents.proposal_runner — match however test_capture_bedrock_failfast imports get_triage_runner (`from api import app, get_triage_runner`), i.e. ensure api.py exposes get_proposal_runner in its namespace and import it the same way.
  </action>
  <verify>
    <automated>cd backend && python3 -m pytest tests/test_advance_endpoint.py tests/test_advance_bedrock_failfast.py -q</automated>
    <fails_when>non-zero exit, or "0 passed" / "no tests ran" in the summary line, or any failed/error count > 0</fails_when>
    <automated>cd backend && python3 -m pytest tests/test_advance_bedrock_failfast.py -q</automated>
    <fails_when>non-zero exit, or a 503 case returns 500/200, or the secret Message text leaks into the response detail</fails_when>
    <automated>cd backend && python3 -m pytest</automated>
    <fails_when>non-zero exit, or any failed/error count > 0 across the full suite</fails_when>
  </verify>
  <acceptance_criteria>
    - 404 (unknown id), 409 (no-triage and skip-verdict), and 400 (unsupported stage) guards are each asserted.
    - The ambiguous-apply engagement escalates at the endpoint (proposal.needs_human_input true + question, contract null) and round-trips via GET (SC2/SC3/SC4).
    - Every injected-runner-failure case returns 503, never 500, and never leaks the raw AWS Message / secret (D-07g).
    - Full suite green.
  </acceptance_criteria>
  <done>The /advance HTTP contract (guards, verbatim merge, credential-free 503) is fully covered and the suite is green.</done>
  <reversibility rating="costly">D-05 endpoint shape + status-code contract (404/409/400/503) is what Phase 6 (stage="ops") and any client build on; changing it later ripples into Phase 6.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API | Untrusted JSON body, UUID path param, and `stage` query param cross into `advance()` |
| API → store | Read-modify-write of the persisted Engagement Record (FastAPI is the sole writer) |
| API → Bedrock | Only on the live path (PROPOSAL_BACKEND=supervisor); this plan's default deterministic path never reaches it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering / Info Disclosure | advance() `engagement_id` path param | low | mitigate | `engagement_id` typed UUID (Pydantic) — path traversal structurally closed; FileEngagementStore._path() raises TypeError on non-UUID (mirrors /capture, T-03-03) |
| T-05-02 | Information Disclosure | advance() 503 exception path | medium | mitigate | Reuse `map_bedrock_error` unmodified (emits exception type / Error.Code only, never the raw AWS Message); asserted in test_advance_bedrock_failfast.py |
| T-05-03 | Information Disclosure (minor) | advance() `stage` guard | low | mitigate | Explicit `if stage != "proposal": raise HTTPException(400)` never branches into unimplemented code; no ops-stage code exists to expose |
| T-05-04 | Tampering (reflected content) | check_scope_clarity `question` | low | accept | Plain JSON API response, no HTML rendering context this phase; `question` is deterministic-template text passing through the Pydantic `str` boundary. Flag for the Chrome-extension render phase if `question` is ever rendered as HTML |
| T-05-SC | Tampering (supply chain) | pip installs | low | accept | No new packages this phase (RESEARCH Package Legitimacy Audit: N/A — all deps pinned in Phase 1's pyproject.toml). No install task exists; no legitimacy checkpoint required |

No `high`-severity threats — nothing blocks (security_block_on: high).
</threat_model>

<verification>
- Full suite green offline with placeholder AWS credentials: `cd backend && python3 -m pytest` (baseline 37 + new Phase-5 tests).
- REC-03 single-writer intact: `cd backend && python3 -m pytest tests/test_single_writer.py -q`.
- SC1: clear-scope apply engagement advance returns proposal + contract + typed payment schedule.
- SC2: ambiguous fixture escalates with a specific question, no structured-output exception.
- SC3: mutual-exclusivity validator rejects both-populated / neither-populated.
- SC4: proposal/contract slices reach the record verbatim via the FastAPI merge only.
- Deterministic default: PROPOSAL_BACKEND unset selects the offline path.
</verification>

<success_criteria>
- PROP-01, PROP-02, PROP-03, PROP-04 delivered on the deterministic path with automated coverage.
- SC1/SC2/SC3/SC4 each proven by a named automated test.
- 404/409/400 guards and credential-free 503 fail-fast asserted.
- No new module under backend/agents or backend/tools imports the store.
- Full test suite green.
</success_criteria>

<output>
Create `.planning/phases/05-proposal-contract-agent-advance-stage-proposal/05-01-SUMMARY.md` when done.
</output>
