"""Throwaway spike scripts (Strands agents-as-tools + Bedrock connectivity).

Never imported by api.py (D-08) — these are standalone verification spikes,
excluded from the API's import path so a missing/invalid AWS credential can
never break the rest of the application.
"""
