---
phase: 05-proposal-contract-agent-advance-stage-proposal
plan: 2
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - backend/agents/proposal_contract_agent.py
  - backend/agents/supervisor.py
  - backend/tests/test_proposal_supervisor_wiring.py
autonomous: true
requirements: [PROP-01, PROP-02, PROP-03, PROP-04]

estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A distinct Proposal-Contract Agent instance is wired into a stage-scoped Supervisor via agents-as-tools, observable as two distinct Agent instances (D-04)."
    - "The app + Supervisor + Proposal-Contract Agent construct without AWS credentials — no eager network call at construction time (D-07(f))."
    - "extract_proposal_result reads the specialist's typed toolResult JSON block and never the Supervisor's re-authored prose (ORC-02/D-02)."
  artifacts:
    - backend/agents/proposal_contract_agent.py
    - backend/agents/supervisor.py
    - backend/tests/test_proposal_supervisor_wiring.py
  key_links:
    - "build_proposal_supervisor() -> build_proposal_contract_agent().as_tool(delegate=True) -> Supervisor Agent tools=[proposal_tool]"
    - "_supervisor_proposal_runner (proposal_runner.py, Plan 05-01) -> build_proposal_supervisor + extract_proposal_result"
    - "build_proposal_contract_agent tools=[check_scope_clarity, draft_proposal, draft_contract], structured_output_model=ProposalContractResult"
  prohibitions:
    - statement: "build_proposal_supervisor does NOT extend build_supervisor() — the existing triage supervisor keeps exactly one tool (avoids two-tool toolUseId disambiguation)."
      status: enforced
      verification: "cd backend && python3 -m pytest tests/test_proposal_supervisor_wiring.py -k unchanged -q — asserts build_supervisor() still has only gig_triage_agent and build_proposal_supervisor() has only proposal_contract_agent"
    - statement: "build_proposal_contract_agent performs no eager network call at construction (constructible without AWS credentials)."
      status: enforced
      verification: "cd backend && python3 -m pytest tests/test_proposal_supervisor_wiring.py -q — construction tests run offline with placeholder creds"
    - statement: "No module under backend/agents imports the store (REC-03/D-05)."
      status: enforced
      verification: "cd backend && python3 -m pytest tests/test_single_writer.py -q"
    - statement: "No code introduces the deprecated Agent.structured_output()/.structured_output_async() method — the specialist uses structured_output_model= at construction."
      status: enforced
      verification: "cd backend && python3 -m pytest tests/test_proposal_supervisor_wiring.py -q (construction asserts structured_output_model wiring); manual code review of proposal_contract_agent.py"
---

<objective>
Add the genuine second specialist for Stage 2: a distinct Proposal-Contract `Agent` wired into a
stage-scoped `build_proposal_supervisor()` via the agents-as-tools pattern Phase 3 proved, plus
`extract_proposal_result` (the typed toolResult channel that Plan 05-01's
`_supervisor_proposal_runner` already lazy-imports). This satisfies D-04 ("two distinct Agent
instances, observable as separate invocations") and D-07(f) (offline construction) WITHOUT
prematurely building ORC-01's unified three-agent Supervisor (that is Phase 6).

Purpose: realize PROP-01..04 on the live path behind the existing `PROPOSAL_BACKEND=supervisor`
seam, keeping the deterministic default (Plan 05-01) as the demo/test path.
Output: 1 new agent file, 1 modified supervisor file (new functions only — `build_supervisor` /
`extract_triage_result` untouched), 1 new offline wiring test file. The live two-agent Bedrock
trace is a documented MANUAL verification (sandbox has placeholder creds), exactly as in Phases
1 and 3.

Depends on Plan 05-01 (needs `ProposalContractResult`, the three tools, and the
`_supervisor_proposal_runner` seam).
</objective>

<execution_context>
@/home/user/Freelance-Autopilot/.claude/gsd-core/workflows/execute-plan.md
@/home/user/Freelance-Autopilot/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-proposal-contract-agent-advance-stage-proposal/05-CONTEXT.md
@.planning/phases/05-proposal-contract-agent-advance-stage-proposal/05-RESEARCH.md

# Phase-3 analogs to mirror:
@backend/agents/gig_triage_agent.py
@backend/agents/supervisor.py
@backend/tests/test_supervisor_wiring.py
# Plan 05-01 artifacts this plan consumes:
@backend/agents/proposal_runner.py
@backend/models/engagement_record.py
</context>

<artifacts_produced>
Symbols and files this plan creates or modifies:
- `MODEL_ID`, `REGION`, `build_proposal_contract_agent` in `backend/agents/proposal_contract_agent.py` (new)
- `build_proposal_supervisor`, `extract_proposal_result` in `backend/agents/supervisor.py` (added — build_supervisor/extract_triage_result unchanged)
- `backend/tests/test_proposal_supervisor_wiring.py` (new offline wiring tests)
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: build_proposal_contract_agent specialist (mirror build_gig_triage_agent)</name>
  <read_first>
    - backend/agents/gig_triage_agent.py (the EXACT analog — MODEL_ID/REGION env pattern, Agent(name=, model=BedrockModel(...), system_prompt=, tools=[...], structured_output_model=...), no eager network call)
    - backend/tools/check_scope_clarity.py, backend/tools/draft_proposal.py, backend/tools/draft_contract.py (Plan 05-01 — the three tools to register)
    - backend/models/engagement_record.py (ProposalContractResult — the structured_output_model)
    - backend/tests/test_supervisor_wiring.py (construction/registration test style; tool_names assertions; no-creds precedent)
    - .planning/phases/05-proposal-contract-agent-advance-stage-proposal/05-RESEARCH.md (State of the Art: use structured_output_model=, NOT the deprecated .structured_output())
  </read_first>
  <files>backend/agents/proposal_contract_agent.py, backend/tests/test_proposal_supervisor_wiring.py</files>
  <behavior>
    - build_proposal_contract_agent() returns a strands Agent whose tool_names include check_scope_clarity, draft_proposal, and draft_contract.
    - Construction performs no network call and succeeds with placeholder AWS credentials.
    - The agent is configured with structured_output_model=ProposalContractResult (the D-01 result model), not the deprecated .structured_output() method.
  </behavior>
  <action>
Create `backend/agents/proposal_contract_agent.py` mirroring gig_triage_agent.py exactly. Module docstring notes construction performs NO network call (only invoking touches Bedrock) — this is what lets the offline construction test pass (D-07(f)) — and that this module must NOT import the store. `from __future__ import annotations`; `import os`; `from strands import Agent`; `from strands.models import BedrockModel`; import `ProposalContractResult` from models.engagement_record and the three tools from tools.*. Reuse the SAME env-var pattern: `MODEL_ID = os.environ.get("BEDROCK_MODEL_ID", "us.anthropic.claude-sonnet-4-6")`, `REGION = os.environ.get("AWS_REGION", "us-east-1")` (do not hardcode a different model id). Define `build_proposal_contract_agent() -> Agent` returning `Agent(name="proposal_contract_agent", model=BedrockModel(model_id=MODEL_ID, region_name=REGION), system_prompt=... (instruct it to call check_scope_clarity first and escalate with needs_human_input + a question when scope/budget is ambiguous rather than guessing, else draft the proposal then the contract), tools=[check_scope_clarity, draft_proposal, draft_contract], structured_output_model=ProposalContractResult)`. Do NOT call the deprecated `agent.structured_output(...)` anywhere.

Create `backend/tests/test_proposal_supervisor_wiring.py` and add the construction tests for THIS task (Task 2 appends supervisor + extract tests): import Agent, build_proposal_contract_agent, ProposalContractResult. Write: (a) build_proposal_contract_agent() returns an Agent instance (constructs without creds — D-07(f)); (b) its tool_names contain check_scope_clarity, draft_proposal, draft_contract. All tests are construction-only — no Bedrock invocation.
  </action>
  <verify>
    <automated>cd backend && python3 -m pytest tests/test_proposal_supervisor_wiring.py -q</automated>
    <fails_when>non-zero exit, or "0 passed" / "no tests ran" in the summary line, or any failed/error count > 0</fails_when>
    <automated>cd backend && python3 -m pytest tests/test_single_writer.py -q</automated>
    <fails_when>non-zero exit, or the REC-03 assertion fails (proposal_contract_agent.py imports the store)</fails_when>
  </verify>
  <acceptance_criteria>
    - build_proposal_contract_agent() constructs offline and returns a strands Agent.
    - The agent registers exactly the three tools (check_scope_clarity, draft_proposal, draft_contract).
    - structured_output_model=ProposalContractResult is used; the deprecated .structured_output() method is absent.
    - proposal_contract_agent.py does not import the store.
  </acceptance_criteria>
  <done>The Proposal-Contract specialist Agent constructs offline with its three tools and typed output model; committed and green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: build_proposal_supervisor + extract_proposal_result (stage-scoped, single-tool)</name>
  <read_first>
    - backend/agents/supervisor.py (build_supervisor lines 22-43 and extract_triage_result lines 46-65 — mirror shapes; do NOT modify them)
    - backend/agents/proposal_contract_agent.py (Task 1 — build_proposal_contract_agent)
    - backend/tests/test_supervisor_wiring.py (the _toolresult_message helper + extract_* behavior tests: reads json block, ignores prose, raises when absent, tolerates malformed blocks)
    - backend/agents/proposal_runner.py (Plan 05-01 — _supervisor_proposal_runner lazy-imports build_proposal_supervisor + extract_proposal_result; the names/signatures must match)
    - .planning/phases/05-proposal-contract-agent-advance-stage-proposal/05-RESEARCH.md (Pattern 3 — stage-scoped supervisor, NOT extending build_supervisor(), to keep the single-scan extract pattern correct)
  </read_first>
  <files>backend/agents/supervisor.py, backend/tests/test_proposal_supervisor_wiring.py</files>
  <behavior>
    - build_proposal_supervisor() returns a Supervisor Agent whose tool_names contain proposal_contract_agent and nothing else.
    - build_supervisor() (unchanged) still contains only gig_triage_agent — it was NOT extended.
    - The Supervisor and the Proposal-Contract specialist are two distinct Agent instances (D-04).
    - extract_proposal_result reads the first toolResult json block and validates it into a ProposalContractResult (both a happy-path payload and an escalation payload); it ignores a conflicting verdict/answer in the Supervisor's own assistant TEXT; it raises RuntimeError when no toolResult json block exists; it tolerates non-dict messages/blocks/toolResults without a raw TypeError.
  </behavior>
  <action>
Add to `backend/agents/supervisor.py` WITHOUT touching build_supervisor / extract_triage_result. Import build_proposal_contract_agent (from agents.proposal_contract_agent) and ProposalContractResult (from models.engagement_record).

Define `build_proposal_supervisor() -> Agent`: construct `proposal_contract_agent = build_proposal_contract_agent()`; wrap it `proposal_tool = proposal_contract_agent.as_tool(name="proposal_contract_agent", description=... (draft a phased proposal + SOW contract + payment schedule for an apply engagement; escalate with needs_human_input + a question when scope/budget is ambiguous — never guess), delegate=True)`; return `Agent(system_prompt="You route every proposal-drafting request to the proposal_contract_agent tool. Never answer yourself.", tools=[proposal_tool])`. Docstring: this is a SEPARATE, stage-scoped supervisor (not build_supervisor extended) so extract_proposal_result's single toolResult scan can never match the wrong specialist (RESEARCH Pattern 3); it performs no network call at construction.

Define `extract_proposal_result(supervisor_messages: list[dict]) -> ProposalContractResult`: mirror extract_triage_result EXACTLY — walk messages, skip non-dict messages/blocks/toolResults/inner-blocks with the same isinstance guards, return `ProposalContractResult.model_validate(content_block["json"])` for the first toolResult content block containing a "json" entry, else `raise RuntimeError("proposal_contract_agent tool result not found in supervisor trace")`. Never read the Supervisor's own final-answer text (D-02).

Append to `backend/tests/test_proposal_supervisor_wiring.py` (mirror test_supervisor_wiring.py): copy the `_toolresult_message` helper. Add: (a) build_proposal_supervisor() registers proposal_contract_agent in tool_names; (b) an "unchanged" test asserting build_supervisor().tool_names contains gig_triage_agent and NOT proposal_contract_agent, and build_proposal_supervisor().tool_names contains proposal_contract_agent and NOT gig_triage_agent (prohibition: build_supervisor not extended); (c) two-distinct-instances: build_proposal_supervisor() is not build_proposal_contract_agent(), both are Agent; (d) extract_proposal_result reads a happy-path json payload into a valid ProposalContractResult (proposal_text/contract_text/payment_schedule present, needs_human_input False); (e) extract_proposal_result reads an escalation json payload (needs_human_input True + question) into a valid ProposalContractResult; (f) it ignores conflicting Supervisor prose (json wins); (g) it raises RuntimeError when no toolResult json block is present; (h) it tolerates malformed/non-dict content blocks and still raises RuntimeError (not TypeError) when no json exists. Use payload dicts that satisfy the D-01 validator (a happy payload needs all three happy fields; an escalation payload needs needs_human_input + question and no happy fields).
  </action>
  <verify>
    <automated>cd backend && python3 -m pytest tests/test_proposal_supervisor_wiring.py -q</automated>
    <fails_when>non-zero exit, or "0 passed" / "no tests ran" in the summary line, or any failed/error count > 0</fails_when>
    <automated>cd backend && python3 -m pytest tests/test_proposal_supervisor_wiring.py -k unchanged -q</automated>
    <fails_when>non-zero exit, or the "unchanged" selection matches 0 tests, or the assertion that build_supervisor was not extended fails</fails_when>
    <automated>cd backend && python3 -m pytest</automated>
    <fails_when>non-zero exit, or any failed/error count > 0 across the full suite</fails_when>
  </verify>
  <acceptance_criteria>
    - build_proposal_supervisor() registers exactly one tool (proposal_contract_agent); build_supervisor() is unchanged (only gig_triage_agent).
    - Supervisor and specialist are two distinct Agent instances (D-04).
    - extract_proposal_result validates both happy and escalation toolResult json payloads into ProposalContractResult, ignores Supervisor prose, and raises RuntimeError (never TypeError) on absent/malformed blocks.
    - Full suite green.
  </acceptance_criteria>
  <done>The stage-scoped Proposal supervisor + typed extraction are wired offline-provably; the seam Plan 05-01 lazy-imports is now complete; suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supervisor → specialist | The typed toolResult JSON channel — the ONLY path FastAPI reads (never the Supervisor's prose) |
| API → Bedrock | Live path only (PROPOSAL_BACKEND=supervisor); manual verification, not exercised offline |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-05 | Information Disclosure | _supervisor_proposal_runner → advance() 503 | medium | mitigate | The live path's Bedrock failures flow through `map_bedrock_error` (type/Code only, never raw Message) — already asserted by Plan 05-01's test_advance_bedrock_failfast.py |
| T-05-06 | Spoofing / Tampering (output integrity) | extract_proposal_result | low | mitigate | Reads the specialist's toolResult json block ONLY, never the Supervisor's re-authored final text (D-02/ORC-02); asserted by the "ignores prose" test |
| T-05-07 | Info Disclosure (minor) | stage-scoped supervisor design | low | mitigate | Single registered tool makes extract_proposal_result's single-scan structurally unable to pick up another specialist's result (RESEARCH Pattern 3) |
| T-05-SC | Tampering (supply chain) | pip installs | low | accept | No new packages this phase (RESEARCH Package Legitimacy Audit: N/A). No install task; no legitimacy checkpoint required |

No `high`-severity threats — nothing blocks (security_block_on: high).
</threat_model>

<verification>
Automated (offline, placeholder creds):
- `cd backend && python3 -m pytest tests/test_proposal_supervisor_wiring.py -q` — construction, registration, distinctness, extract_proposal_result behaviors.
- `cd backend && python3 -m pytest tests/test_single_writer.py -q` — new agent file does not import the store.
- `cd backend && python3 -m pytest` — full suite green.

Manual (live two-agent Bedrock trace — MANUAL ONLY, sandbox has placeholder creds; mirrors Phases 1 & 3):
1. Export real AWS credentials + region and a valid Bedrock inference-profile model id.
2. `export PROPOSAL_BACKEND=supervisor`.
3. POST /capture an apply-verdict job, then POST /engagements/{id}/advance?stage=proposal.
4. Inspect the trace: two distinct Agent invocations (stage-scoped Supervisor and the Proposal-Contract specialist), and the persisted proposal/contract slices matching the specialist's typed toolResult output verbatim.
</verification>

<success_criteria>
- D-04: a distinct Proposal-Contract Agent is wired into a stage-scoped Supervisor via agents-as-tools (two distinct instances), without extending build_supervisor() or building ORC-01.
- D-07(f): app + Supervisor + Proposal-Contract Agent construct without AWS credentials.
- ORC-02/D-02: extract_proposal_result reads the typed toolResult channel, never the Supervisor's prose.
- The deprecated Agent.structured_output() method is not introduced.
- Full test suite green; live two-agent trace documented as a manual verification.
</success_criteria>

<output>
Create `.planning/phases/05-proposal-contract-agent-advance-stage-proposal/05-02-SUMMARY.md` when done.
</output>
