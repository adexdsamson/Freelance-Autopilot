---
phase: 07-full-demo-verification-submission-docs
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - README.md
  - LICENSE
  - docs/architecture.md
  - docs/demo-script.md
  - backend/tests/test_submission_presence.py
autonomous: true
requirements: [DEMO-03, DEMO-04, DEMO-05]
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The repo root carries a visible OSI license (MIT) and a README with Setup / Run / Test sections that cite the verified-working commands (DEMO-03, DEMO-04)."
    - "docs/architecture.md contains a Mermaid diagram naming only symbols that exist in backend/, and docs/demo-script.md is a <=5-minute walkthrough tied to run_demo.py's exact commands (DEMO-05)."
    - "A pytest presence test resolves the TRUE repo root and asserts LICENSE, README.md, docs/architecture.md (with a mermaid fenced block), and docs/demo-script.md all exist with the required shape (D-07(c)(d))."
    - "No AWS credential, secret, or email address appears in README.md, LICENSE, or docs/* — the live-Bedrock note stays generic (T-07-SECRET)."
  artifacts:
    - README.md
    - LICENSE
    - docs/architecture.md
    - docs/demo-script.md
    - backend/tests/test_submission_presence.py
  key_links:
    - "test_submission_presence.py resolves the repo root via Path(__file__).resolve().parents[2] (backend/tests -> backend -> repo root) — one level beyond test_single_writer.py's backend/ root."
    - "README + demo-script cite `cd backend && python3 -m scripts.run_demo` and `cd backend && uvicorn api:app --reload` — the verified-working forms, never the ModuleNotFoundError forms (RESEARCH.md Pitfalls 2 & 3)."
    - "docs/architecture.md is grounded in the real object graph: build_full_supervisor + gig_triage_agent/proposal_contract_agent/ops_agent as agents-as-tools; the Triage/Proposal/Ops runner seams; api.py as sole store writer over EngagementStore/FileEngagementStore; the extension drawn dashed (Phase 4)."
---

<objective>
Make the repository a submittable hackathon entry: a README with accurate setup/run/test instructions, an OSI license at the repo root, a Mermaid architecture diagram matching the real object graph, and a <=5-minute demo script — all verified present by an automated test.

**MVP user story (faithful reframing of the ROADMAP Phase 7 Goal):**
**As a** hackathon submitter, **I want to** ship a repo whose About section shows a license and whose README, architecture diagram, and demo script let a judge clone, understand, run, and evaluate the entry, **so that** the submission satisfies every "Fixtures, Demo & Submission" requirement without the judge needing to reverse-engineer the codebase.

Purpose: DEMO-03/04/05 are the submission-checklist requirements. They must be accurate against the real code (no invented commands or symbols) and machine-verified so a later edit can't silently drop an artifact.
Output: `README.md` and `LICENSE` (repo root), `docs/architecture.md` and `docs/demo-script.md`, and `backend/tests/test_submission_presence.py` proving all four exist with the required shape.
</objective>

<execution_context>
@/home/user/Freelance-Autopilot/.claude/gsd-core/workflows/execute-plan.md
@/home/user/Freelance-Autopilot/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-full-demo-verification-submission-docs/07-CONTEXT.md
@.planning/phases/07-full-demo-verification-submission-docs/07-RESEARCH.md
@.planning/phases/07-full-demo-verification-submission-docs/07-PATTERNS.md

# Ground truth for the README run-instructions and the architecture diagram symbols:
@backend/api.py
@backend/agents/supervisor.py
@backend/requirements.txt
@backend/scripts/run_demo.py

# Tone/section analogs for the docs:
@docs/PRD.md
</context>

## Artifacts this phase produces

The full Phase 7 artifact set (delivered across Plans 07-01 and 07-02):

| Artifact | Plan | Requirement |
|----------|------|-------------|
| `backend/scripts/run_demo.py` | 07-01 | DEMO-02 |
| `backend/tests/test_demo_determinism.py` | 07-01 | DEMO-02 |
| `backend/tests/test_submission_presence.py` | 07-02 | DEMO-03/04/05 |
| `README.md` (repo root) | 07-02 | DEMO-03 |
| `LICENSE` (repo root) | 07-02 | DEMO-04 |
| `docs/architecture.md` | 07-02 | DEMO-05 |
| `docs/demo-script.md` | 07-02 | DEMO-05 |

**Label mapping (reconciliation):** the authoritative `.planning/REQUIREMENTS.md` assigns DEMO-04 = "An OSI license is present at the repo root" and DEMO-05 = "An architecture diagram and a demo script are included." CONTEXT.md's decision letters (D-04 = license, D-05 = diagram+script) happen to align with the artifacts but are offset by one from the REQ-ID numerals in some notes; this plan maps by CONTENT per REQUIREMENTS.md. All four artifacts are delivered regardless of numeral.

<tasks>

<task type="auto">
  <name>Task 1: LICENSE (MIT) + README.md at repo root (DEMO-04 license, DEMO-03 README; D-03/D-04)</name>
  <files>LICENSE, README.md</files>
  <reversibility rating="reversible">The LICENSE copyright-holder string defaults to `Copyright (c) 2026 Freelance Autopilot` and is user-adjustable (D-04) — a one-line edit if the owner wants a personal/company name. No email address goes in the file.</reversibility>
  <read_first>
    - RESEARCH.md Assumptions Log A1 (MIT standard text shape) and the "What NOT to Use" note; CONTEXT.md D-04 (MIT, `Copyright (c) 2026 Freelance Autopilot`, holder user-adjustable, no email).
    - RESEARCH.md Pitfalls 2 & 3 — the ONLY verified-working commands: demo entrypoint `cd backend && python3 -m scripts.run_demo`; server `cd backend && uvicorn api:app --reload`; tests `cd backend && python3 -m pytest`. The forms `python3 backend/scripts/run_demo.py` and `uvicorn backend/api.py:app` FAIL — never cite them.
    - backend/requirements.txt (pinned deps: strands-agents==1.54.0, pydantic, boto3, fastapi, uvicorn[standard], httpx, pytest) and backend/pyproject.toml requires-python = ">=3.10".
    - backend/api.py routes and module docstring (name endpoints exactly: `POST /capture`, `GET /engagements/{engagement_id}`, `POST /engagements/{engagement_id}/advance?stage=proposal|ops`).
    - backend/scripts/run_demo.py (Plan 07-01) — the actual `--fixture`/`--ambiguous` CLI contract the README run section must match.
    - .planning/REQUIREMENTS.md (for the requirements-traceability table IDs).
  </read_first>
  <action>
    Create the repo-root LICENSE with the standard MIT license text, copyright line `Copyright (c) 2026 Freelance Autopilot`. Use the canonical MIT wording (the "Permission is hereby granted, free of charge..." body). Put NO email address and NO AWS credential anywhere in the file (T-07-SECRET). Add a one-line comment or note is not needed inside the license body; the user-adjustable copyright holder is noted in the README instead.

    Create the repo-root README.md accurate against the real code (no invented commands or paths). Include, in a sensible order (ordering is Claude's discretion per CONTEXT.md): a one-line project value proposition; an Architecture overview paragraph linking docs/architecture.md; a Setup section (Python 3.10+, `pip install -r backend/requirements.txt`, and a generic optional AWS-credential note for the live `*_BACKEND=supervisor` path — describe it as "configure AWS credentials via the standard boto3 chain" WITHOUT printing any key, secret, or email, T-07-SECRET); a Run section citing the verified demo command `cd backend && python3 -m scripts.run_demo --fixture creep` (and `--fixture clean` / `--ambiguous`) and the server command `cd backend && uvicorn api:app --reload`; a Test section citing `cd backend && python3 -m pytest`; a short requirements-traceability table pulling IDs from REQUIREMENTS.md (at minimum DEMO-02/03/04/05, ORC-01/02, REC-03, API-03); and a Manual-only boundaries section documenting the three genuine out-of-scope items — the Chrome MV3 extension capture front (Phase 4 dependency, not built), the recorded video (a human action this phase ships only the script for), and the live four-agent Bedrock trace (needs real AWS creds; set them plus `TRIAGE_BACKEND`/`PROPOSAL_BACKEND`/`OPS_BACKEND=supervisor`). The README MUST contain the literal headings Setup, Run, and Test (the presence test in Task 3 checks for them, case-insensitively). Note the LICENSE copyright holder is user-adjustable.
  </action>
  <verify>
    <automated>cd /home/user/Freelance-Autopilot && test -f LICENSE && test -f README.md && grep -q "Permission is hereby granted" LICENSE && echo PRESENT</automated>
    <fails_when>output is not "PRESENT" (a file is missing or LICENSE lacks the MIT grant clause), or non-zero exit</fails_when>
  </verify>
  <verify>
    <automated>cd /home/user/Freelance-Autopilot && grep -qi "setup" README.md && grep -qi "run" README.md && grep -qi "test" README.md && echo SECTIONS_OK</automated>
    <fails_when>output is not "SECTIONS_OK" (a required Setup/Run/Test heading is missing)</fails_when>
  </verify>
  <verify>
    <automated>cd /home/user/Freelance-Autopilot && ! grep -RniE "aws_secret_access_key|aws_access_key_id|ai@mainlandtech.com" README.md LICENSE && echo NO_SECRETS</automated>
    <fails_when>output is not "NO_SECRETS" (a credential literal or email address leaked into README/LICENSE, T-07-SECRET)</fails_when>
  </verify>
  <acceptance_criteria>
    - DEMO-04: a repo-root LICENSE with valid MIT text and copyright line `Copyright (c) 2026 Freelance Autopilot` exists; no email/secret in it.
    - DEMO-03: a repo-root README.md exists with Setup, Run, and Test sections citing only the verified-working commands (`python3 -m scripts.run_demo`, `uvicorn api:app --reload`, `python3 -m pytest`), plus a traceability table and the three manual-only boundary notes.
    - T-07-SECRET: no AWS credential literal or email address appears in README.md or LICENSE.
  </acceptance_criteria>
  <done>LICENSE and README.md exist at the repo root, are accurate against the code, and are committed.</done>
</task>

<task type="auto">
  <name>Task 2: docs/architecture.md (Mermaid, real object graph) + docs/demo-script.md (<=5-min walkthrough) (DEMO-05; D-05/D-06)</name>
  <files>docs/architecture.md, docs/demo-script.md</files>
  <read_first>
    - RESEARCH.md "System Architecture Diagram" — the verified Mermaid flowchart built by reading every file it depicts this session; reuse it as the diagram base, with one correction: the runner seams default to the env var VALUE "placeholder" (not "unset") and switch to the live path when `*_BACKEND=supervisor` (verified in backend/agents/{triage,proposal,ops}_runner.py: `os.environ.get("<X>_BACKEND", "placeholder")`).
    - backend/agents/supervisor.py — the real symbols to name: build_full_supervisor() (ORC-01, wires gig_triage_agent + proposal_contract_agent + ops_agent via `.as_tool(..., delegate=True)`); build_supervisor / build_proposal_supervisor (the stage-scoped variants); extract_triage_result / extract_proposal_result / extract_ops_result.
    - backend/api.py — endpoints and the sole-store-writer contract (get_store -> FileEngagementStore; only api.py imports the store); the runner DI seams get_triage_runner / get_proposal_runner / get_ops_runner.
    - backend/store/ (EngagementStore ABC, FileEngagementStore impl) and backend/fixtures/loader.py (load_client_thread / load_payment_schedule).
    - backend/scripts/run_demo.py (Plan 07-01) — the exact `--fixture creep|clean` / `--ambiguous` commands each demo beat runs.
    - RESEARCH.md D-06 beats + Assumptions Log A2 (the live-trace mechanism: `*_BACKEND=supervisor` env vars + backend/scripts/smoke_test_bedrock_connectivity.py precedent).
    - backend/tests/test_advance_endpoint.py — confirms creep fixture -> exactly 2 ops cards, clean -> 0, and the ambiguous input text for beat 3.
  </read_first>
  <action>
    Create docs/architecture.md containing a Mermaid `flowchart` fenced block (opened with a triple-backtick mermaid fence so it renders natively on GitHub, D-05) plus a short component table. The diagram must name ONLY symbols that exist in backend/ (verified in the read_first files): the Chrome MV3 extension drawn dashed/planned (Phase 4, not built) posting to `/capture`; the FastAPI app (api.py) as the sole EngagementStore writer exposing `/capture`, `GET /engagements/{id}`, `/advance?stage=proposal|ops`; run_demo.py driving the app in-process via TestClient; the three runner DI seams (get_triage_runner/get_proposal_runner/get_ops_runner) each with a deterministic default (`*_BACKEND=placeholder`) and a live path (`*_BACKEND=supervisor`) into build_full_supervisor(); the unified Supervisor orchestrating gig_triage_agent, proposal_contract_agent, and ops_agent as agents-as-tools (ORC-01), each reaching Amazon Bedrock only on the live path (dashed); the deterministic ops path reading fixtures via fixtures/loader.py; and the store writes flowing api.py -> EngagementStore -> FileEngagementStore. Do NOT invent symbols or copy the PRD's aspirational tree — the diagram is judged against the real object graph.

    Create docs/demo-script.md — an ordered <=5-minute recorded-walkthrough script with narration plus the exact command per beat, driven entirely by run_demo.py's real CLI (D-06): Beat 1 — `cd backend && python3 -m scripts.run_demo --fixture creep` -> narrate the triage apply verdict, the drafted proposal + contract, and the 2 ops escalation cards (1 scope-creep message + 1 overdue milestone). Beat 2 — `cd backend && python3 -m scripts.run_demo --fixture clean` -> narrate 0 ops cards, proving the checks are conditional, not hardcoded. Beat 3 — `cd backend && python3 -m scripts.run_demo --ambiguous` -> narrate the needs_human_input escalation (the ambiguous job "Looking for someone to help with ongoing design work." produces no contract). Beat 4 — note the live four-agent Bedrock trace as the manual step: set AWS credentials via the standard boto3 chain and export `TRIAGE_BACKEND=supervisor PROPOSAL_BACKEND=supervisor OPS_BACKEND=supervisor`, then run the same flow (referencing the smoke_test_bedrock_connectivity.py precedent). Put NO credential literal or email in the script (T-07-SECRET) — the live-path note stays generic. Explicitly flag that the Chrome-extension capture front (Phase 4) and the recorded video itself are out of scope for this script.
  </action>
  <verify>
    <automated>cd /home/user/Freelance-Autopilot && test -f docs/architecture.md && test -f docs/demo-script.md && grep -q '```mermaid' docs/architecture.md && echo DOCS_OK</automated>
    <fails_when>output is not "DOCS_OK" (a doc is missing or architecture.md lacks a ```mermaid fenced block), or non-zero exit</fails_when>
  </verify>
  <verify>
    <automated>cd /home/user/Freelance-Autopilot && grep -q "build_full_supervisor" docs/architecture.md && grep -q "FileEngagementStore" docs/architecture.md && grep -q "run_demo" docs/demo-script.md && echo GROUNDED</automated>
    <fails_when>output is not "GROUNDED" (the diagram does not name build_full_supervisor / FileEngagementStore, or the demo script does not reference run_demo)</fails_when>
  </verify>
  <verify>
    <automated>cd /home/user/Freelance-Autopilot && ! grep -RniE "aws_secret_access_key|aws_access_key_id|ai@mainlandtech.com" docs/architecture.md docs/demo-script.md && echo NO_SECRETS</automated>
    <fails_when>output is not "NO_SECRETS" (a credential literal or email leaked into a doc, T-07-SECRET)</fails_when>
  </verify>
  <acceptance_criteria>
    - DEMO-05: docs/architecture.md has a Mermaid fenced block naming real backend/ symbols (build_full_supervisor, the three specialists as agents-as-tools, the runner seams, api.py as sole store writer, EngagementStore/FileEngagementStore, fixtures/loader.py), with the extension drawn dashed/planned.
    - DEMO-05: docs/demo-script.md is a <=5-minute walkthrough whose four beats each cite a real run_demo.py command (creep -> 2 cards, clean -> 0 cards, ambiguous -> needs_human_input, live Bedrock trace as manual step).
    - T-07-SECRET: no credential literal or email in either doc; the live-path note is generic.
  </acceptance_criteria>
  <done>docs/architecture.md and docs/demo-script.md exist, are grounded in the real object graph and the real CLI, and are committed.</done>
</task>

<task type="auto">
  <name>Task 3: test_submission_presence.py — machine-verify all submission artifacts (DEMO-03/04/05; D-07(c)(d))</name>
  <files>backend/tests/test_submission_presence.py</files>
  <read_first>
    - RESEARCH.md Code Examples "Presence test skeleton (D-07(c)/(d))" — the exact `REPO_ROOT = Path(__file__).resolve().parents[2]` resolution and the four assertions; reuse them.
    - PATTERNS.md "Repo-root / backend-root Path resolution" — the off-by-one warning: test_single_writer.py's repo_root lands on backend/ (parent.parent); this file needs ONE MORE .parent (parents[2]) to reach the TRUE repo root where README.md / LICENSE / docs/ live.
    - backend/tests/test_single_writer.py — the plain pathlib + assert-with-message + requirement-ID-docstring idiom to match.
  </read_first>
  <action>
    Create backend/tests/test_submission_presence.py with a module docstring citing DEMO-03/DEMO-04/DEMO-05/D-07(c)(d). Resolve the TRUE repo root as `Path(__file__).resolve().parents[2]` (backend/tests -> backend -> repo root) — NOT parent.parent, which would stop at backend/. Write small, separate test functions using pathlib and assert-with-message: (1) LICENSE exists at repo root and its text contains "MIT License" or "Permission is hereby granted"; (2) README.md exists at repo root and its lowercased text contains "setup", "run", and "test"; (3) docs/architecture.md exists and its text contains a triple-backtick mermaid fence; (4) docs/demo-script.md exists. Keep it presence/substring checking only — no ast, no import-graph analysis (that pattern is specific to test_single_writer.py's need). This test joins the existing `cd backend && python3 -m pytest` suite.
  </action>
  <verify>
    <automated>cd backend && python3 -m pytest tests/test_submission_presence.py -q</automated>
    <fails_when>non-zero exit, or "failed" / "0 passed" appears in the pytest summary line (a missing/mis-shaped artifact fails a presence assertion)</fails_when>
  </verify>
  <acceptance_criteria>
    - D-07(c): the test asserts repo-root LICENSE (MIT text) and README.md (with Setup/Run/Test) exist, resolving the TRUE repo root via parents[2].
    - D-07(d): the test asserts docs/architecture.md exists with a ```mermaid block and docs/demo-script.md exists.
    - The test passes inside the existing offline pytest suite.
  </acceptance_criteria>
  <done>test_submission_presence.py passes, resolves the correct repo root, and is committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo -> public reader (judge) | README, LICENSE, and docs/* are public in a submitted repo; they must not embed any credential, secret, or personal email. |
| presence test -> filesystem | test_submission_presence.py only reads files (Path.exists / read_text); it introduces no write or import-graph surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-SECRET | Information Disclosure | README.md / LICENSE / docs/architecture.md / docs/demo-script.md | high | mitigate | Keep the live-Bedrock note generic ("configure AWS credentials via the standard boto3 chain" + `*_BACKEND=supervisor`); embed no key/secret literal and no email address; each doc task has an automated negative-grep verify for `aws_secret_access_key`/`aws_access_key_id`/the user's email. |
| T-07-DOCDRIFT | Tampering (inaccurate submission artifact) | README run commands / architecture diagram symbols | medium | mitigate | Cite only the RESEARCH-verified commands (`python3 -m scripts.run_demo`, `uvicorn api:app --reload`); ground the diagram in real backend/ symbols (grep-verified for build_full_supervisor / FileEngagementStore); machine-verify presence + shape with test_submission_presence.py so a later edit can't silently drop an artifact. |
| T-07-SC | Tampering | npm/pip/cargo installs | n/a | accept | No new external packages this phase (RESEARCH.md Package Legitimacy Audit: N/A); no install task, so no package-legitimacy checkpoint is required. |

No new authentication, session, access-control, input-validation, or cryptography surface is introduced (RESEARCH.md Security Domain: all ASVS categories "no"). ASVS L1; block on high — T-07-SECRET is enforced by an automated negative-grep gate on every doc.
</threat_model>

<verification>
Full suite (existing 118 + Plan 07-01's determinism tests + this plan's presence test, with the hardened single-writer guard) must be green:

`cd backend && python3 -m pytest -q` — expect all passed, 0 failed.

All four submission artifacts present and correctly shaped:
`cd /home/user/Freelance-Autopilot && test -f README.md && test -f LICENSE && test -f docs/architecture.md && test -f docs/demo-script.md && grep -q '```mermaid' docs/architecture.md && echo ALL_PRESENT` — expect `ALL_PRESENT`.
</verification>

<success_criteria>
- DEMO-03: repo-root README with accurate Setup/Run/Test instructions.
- DEMO-04: repo-root OSI license (MIT).
- DEMO-05: architecture diagram (Mermaid, real object graph) + docs/demo-script.md.
- All four artifacts machine-verified present by test_submission_presence.py in the offline suite.
- T-07-SECRET: no credential or email leaks into any public doc.
</success_criteria>

<output>
Create `.planning/phases/07-full-demo-verification-submission-docs/07-02-SUMMARY.md` when done.
</output>
