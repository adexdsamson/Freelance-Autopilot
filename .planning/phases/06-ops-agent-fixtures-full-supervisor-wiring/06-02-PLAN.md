---
phase: 06-ops-agent-fixtures-full-supervisor-wiring
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - backend/agents/ops_agent.py
  - backend/agents/supervisor.py
  - backend/tests/test_full_supervisor_wiring.py
autonomous: true
requirements: [ORC-01]

estimate:
  tokens: 38000
  raw_tokens: 24000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "build_full_supervisor() registers all three specialist tools (gig_triage_agent, proposal_contract_agent, ops_agent) and yields four distinct Agent instances — ORC-01/SC1, D-01"
    - "extract_ops_result disambiguates the ops_agent toolResult by tool name from a multi-tool trace, never reading the Supervisor's own prose — ORC-01, D-01"
    - "build_supervisor and build_proposal_supervisor remain single-tool and unchanged (additive third builder only) — D-01"
    - "build_ops_agent constructs offline with no network call and sets structured_output_model=OpsResult — D-01, Pitfall 4"
  artifacts:
    - backend/agents/ops_agent.py
    - backend/agents/supervisor.py
    - backend/tests/test_full_supervisor_wiring.py
  key_links:
    - "build_full_supervisor → build_ops_agent (structured_output_model=OpsResult) → .as_tool(name='ops_agent', delegate=True) → supervisor tools list (D-01)"
    - "_supervisor_ops_runner (06-01) lazy-imports build_full_supervisor + extract_ops_result (this plan) — completing the live path (D-03)"
    - "_find_tool_result_json two-pass: index toolUse.name→toolUseId, then filter toolResult by toolUseId (RESEARCH Pattern 5)"
---

<objective>
**MVP user story (from the ROADMAP Phase 6 Goal + PROJECT.md core value):**
**As a** reviewer/judge of the system, **I want to** see one Supervisor orchestrate three
distinct specialist agents as four independently traceable invocations, **so that** the
multi-agent orchestration is genuinely visible in the code — not one wrapped LLM call.

This plan builds the ORC-01 live-orchestration wiring on top of Plan 06-01's ops stage: the
Ops specialist `build_ops_agent()` (D-02, mirrors the two existing specialists), the unified
`build_full_supervisor()` that registers ALL THREE specialists as agents-as-tools on ONE
Supervisor (D-01/SC1), and the name-disambiguated `extract_ops_result` / shared
`_find_tool_result_json` two-pass extractor that generalizes the Phase 3/5 first-json-block
scan for a multi-tool trace (D-01 — the toolUseId↔name disambiguation Phase 5 deferred).

Purpose: This is the "genuine multi-agent orchestration visible in code" the judges score
(SC1). It is provable OFFLINE by construction + extraction tests (no AWS creds); the live
four-agent Bedrock trace is documented manual verification, exactly as in Phases 1/3/5 (D-08).
Output: A single Supervisor whose tools list holds all three specialists, plus a robust
by-name extractor that Plan 06-01's `_supervisor_ops_runner` lazy-imports.
</objective>

<execution_context>
@/home/user/Freelance-Autopilot/.claude/gsd-core/workflows/execute-plan.md
@/home/user/Freelance-Autopilot/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ops-agent-fixtures-full-supervisor-wiring/06-CONTEXT.md
@.planning/phases/06-ops-agent-fixtures-full-supervisor-wiring/06-RESEARCH.md
@.planning/phases/06-ops-agent-fixtures-full-supervisor-wiring/06-PATTERNS.md
@backend/agents/supervisor.py
</context>

## Artifacts this plan produces

New symbols/files (all under git-tracked `backend/`):
- `backend/agents/ops_agent.py` — `build_ops_agent()` (mirrors build_proposal_contract_agent; tools=[check_scope_creep, check_invoice_status, draft_status_update], structured_output_model=OpsResult, D-02/D-01).
- `backend/agents/supervisor.py` — ADD `build_full_supervisor()`, the shared `_find_tool_result_json(messages, tool_name)` two-pass helper, and `extract_ops_result(messages)` (D-01). KEEP `build_supervisor`/`build_proposal_supervisor`/`extract_triage_result`/`extract_proposal_result` unchanged.
- `backend/tests/test_full_supervisor_wiring.py` — ORC-01/SC1 construction + name-disambiguation + prohibition tests.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: TRACER — build_ops_agent + build_full_supervisor + name-disambiguated extract_ops_result, wired end-to-end offline</name>
  <files>backend/agents/ops_agent.py, backend/agents/supervisor.py, backend/tests/test_full_supervisor_wiring.py</files>
  <read_first>
    - backend/agents/supervisor.py (the FULL analog: build_supervisor + build_proposal_supervisor at lines 23-95 to add a third builder alongside; extract_triage_result/extract_proposal_result at lines 47-66 and 98-117 — the single-tool first-json-block scan this plan GENERALIZES by tool name; the load-bearing docstring at lines 1-13)
    - backend/agents/proposal_contract_agent.py (the specialist-builder analog to mirror EXACTLY: name=, model=BedrockModel(model_id, region_name), system_prompt sequencing the tool calls, tools=[...], structured_output_model=...)
    - backend/models/engagement_record.py (OpsResult — authored in Plan 06-01 — is the structured_output_model and the extractor's validation target)
    - backend/tools/check_scope_creep.py, backend/tools/check_invoice_status.py, backend/tools/draft_status_update.py (the three tools — authored in Plan 06-01 — that build_ops_agent registers)
    - backend/tests/test_proposal_supervisor_wiring.py (the construction-only test shapes to mirror: isinstance(agent, Agent), "tool_name" in agent.tool_names, distinct-instance checks, and the `_toolresult_message` helper at lines 61-73)
    - .planning/phases/06-ops-agent-fixtures-full-supervisor-wiring/06-RESEARCH.md (Pattern 1 build_full_supervisor, Pattern 5 the verbatim two-pass extractor, Pitfall 4 structured_output_model requirement)
  </read_first>
  <behavior>
    - `build_ops_agent()` returns an `Agent` with `check_scope_creep`, `check_invoice_status`, `draft_status_update` in its `tool_names` and no network call at construction.
    - `build_full_supervisor()` returns an `Agent` whose `tool_names` contains all three of `gig_triage_agent`, `proposal_contract_agent`, `ops_agent`.
    - `build_full_supervisor()` plus the three specialist builders yield four DISTINCT `Agent` instances (SC1).
    - `extract_ops_result(messages)` on a trace where an assistant `toolUse` names `ops_agent` with toolUseId X and a following `toolResult` carries toolUseId X with a `json` block validates that json into an `OpsResult`.
    - `extract_ops_result(messages)` raises `RuntimeError` when no `ops_agent` toolResult json block is present.
  </behavior>
  <action>
    Wire the complete live-path skeleton offline — specialist → unified supervisor → by-name extraction — proving the ORC-01 architecture end-to-end without Bedrock.

    (1) `backend/agents/ops_agent.py` (D-02/D-01): mirror proposal_contract_agent.py exactly. Import `os`, `Agent`, `BedrockModel`, `OpsResult` from models.engagement_record, and the three ops tools. Reuse the `MODEL_ID`/`REGION` env pattern (`os.environ.get("BEDROCK_MODEL_ID", "us.anthropic.claude-sonnet-4-6")`, `os.environ.get("AWS_REGION", "us-east-1")`). Copy the "construction performs NO network call" and "must NOT import the store" docstring notes. `build_ops_agent()` returns `Agent(name="ops_agent", model=BedrockModel(model_id=MODEL_ID, region_name=REGION), system_prompt=<sequence: always call check_scope_creep and check_invoice_status, then draft_status_update reflecting the active flags, and return the typed OpsResult>, tools=[check_scope_creep, check_invoice_status, draft_status_update], structured_output_model=OpsResult)`. The `structured_output_model=OpsResult` is REQUIRED (Pitfall 4 — without it the live toolResult carries text not json).

    (2) `backend/agents/supervisor.py` — ADD (do NOT modify or delete `build_supervisor`, `build_proposal_supervisor`, `extract_triage_result`, `extract_proposal_result`, D-01): import `build_ops_agent` and `OpsResult`. Add `build_full_supervisor() -> Agent` (RESEARCH Pattern 1): build all three specialists via `build_gig_triage_agent()`, `build_proposal_contract_agent()`, `build_ops_agent()`; wrap each with `.as_tool(name=..., description=..., delegate=True)` using the three names `gig_triage_agent`/`proposal_contract_agent`/`ops_agent`; return `Agent(system_prompt=<route each request to EXACTLY ONE specialist tool by stage; never answer yourself; never call more than one specialist per turn>, tools=[triage_tool, proposal_tool, ops_tool])`. <reversibility rating="costly">This is the load-bearing "genuine multi-agent orchestration" contract the judges score; the tools-list shape and the extractor are what Plan 06-01's live runner depends on.</reversibility>

    (3) Add the shared name-disambiguated extractor (RESEARCH Pattern 5 — copy the verbatim two-pass): `_find_tool_result_json(messages: list[dict], tool_name: str) -> dict` does PASS 1 — walk every message/content block, and for each dict `toolUse` whose `name == tool_name` collect its `toolUseId` into a set; PASS 2 — walk again for `toolResult` blocks whose `toolUseId` is in that set, and return the first `content` block that has a `"json"` key. Raise `RuntimeError(f"{tool_name} tool result not found in supervisor trace")` if none. Preserve the EXACT defensive `isinstance` guards on message/block/toolResult/content_block (never index blindly). Then `extract_ops_result(messages) -> OpsResult` returns `OpsResult.model_validate(_find_tool_result_json(messages, "ops_agent"))`. Leave `extract_triage_result`/`extract_proposal_result` UNTOUCHED (they remain correct for their single-tool supervisors — RESEARCH Open Question 1 recommendation).

    (4) `backend/tests/test_full_supervisor_wiring.py` (construction-only + pure-function, NO Bedrock, mirrors test_proposal_supervisor_wiring.py): assert `build_ops_agent()` is an `Agent` with the three ops tools in `tool_names`; assert `build_full_supervisor()` is an `Agent` whose `tool_names` contains all three specialist names (SC1/ORC-01); assert four distinct `Agent` instances exist (supervisor `is not` each specialist, and specialists are pairwise distinct — SC1); use a `_toolresult_message`-style helper with a matching assistant `toolUse` block to assert `extract_ops_result` reads the ops_agent json into an `OpsResult`; assert `extract_ops_result` raises `RuntimeError` when absent.
  </action>
  <verify>
    <automated>cd backend && python3 -m pytest tests/test_full_supervisor_wiring.py -q</automated>
    <fails_when>non-zero exit, or "failed"/"error" in the pytest summary line, or "0 passed"</fails_when>
    <automated>cd backend && python3 -m pytest tests/test_single_writer.py -q</automated>
    <fails_when>non-zero exit, or the REC-03 violation message naming backend/agents/ops_agent.py appears in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - `backend/agents/ops_agent.py` exists, sets `structured_output_model=OpsResult`, does not import the store, and construction makes no network call.
    - `build_full_supervisor().tool_names` contains `gig_triage_agent`, `proposal_contract_agent`, and `ops_agent`.
    - `_find_tool_result_json` and `extract_ops_result` exist in supervisor.py with the two-pass toolUse-index / toolResult-filter algorithm and the defensive `isinstance` guards preserved.
    - `extract_triage_result` and `extract_proposal_result` bodies are unchanged from their current form.
    - test_full_supervisor_wiring.py green.
  </acceptance_criteria>
  <done>The unified three-specialist supervisor and the name-disambiguated ops extractor are wired and proven offline; four distinct Agent instances exist; committed. Plan 06-01's `_supervisor_ops_runner` lazy import now resolves.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extraction robustness + prohibition — multi-tool disambiguation, malformed tolerance, existing supervisors unchanged</name>
  <files>backend/agents/supervisor.py, backend/tests/test_full_supervisor_wiring.py</files>
  <read_first>
    - backend/agents/supervisor.py (the extractor + builders authored in Task 1; this task hardens correctness and adds the prohibition test — do not change build_supervisor/build_proposal_supervisor)
    - backend/tests/test_proposal_supervisor_wiring.py (the two anti-regression analogs to mirror: `test_build_supervisor_unchanged_not_extended` lines 37-48, `test_extract_proposal_result_ignores_supervisor_prose` lines 115-136, and `test_extract_proposal_result_tolerates_malformed_content_blocks` lines 149-167)
    - .planning/phases/06-ops-agent-fixtures-full-supervisor-wiring/06-RESEARCH.md (Anti-Patterns "First-toolResult-wins scan on a multi-tool supervisor" — the exact bug this task's disambiguation test guards against)
  </read_first>
  <behavior>
    - Given a multi-tool trace containing a `proposal_contract_agent` toolResult json FIRST and an `ops_agent` toolResult json SECOND, `extract_ops_result` returns the OPS json — proving it disambiguates by name and does NOT return the first json block found (the D-01 bug guard).
    - `extract_ops_result` returns the ops json even when the Supervisor's own assistant TEXT contains different prose (never reads re-authored prose — D-02).
    - `extract_ops_result` tolerates non-dict messages / content blocks / toolResult payloads and raises `RuntimeError` (never a raw `TypeError`) when no valid ops json block exists among the noise.
    - `build_supervisor()` keeps exactly `gig_triage_agent` (not proposal/ops); `build_proposal_supervisor()` keeps exactly `proposal_contract_agent` (not triage/ops) — the new full supervisor did not mutate them (D-01 prohibition).
  </behavior>
  <action>
    Add hardening + prohibition tests to test_full_supervisor_wiring.py. Author `test_extract_ops_result_disambiguates_by_name_among_multiple_toolresults`: build a `messages` list with a proposal_contract_agent toolUse+toolResult (json = a proposal-shaped payload) appearing BEFORE an ops_agent toolUse+toolResult (json = a valid OpsResult-shaped payload), assert `extract_ops_result` returns the OPS payload (RESEARCH Anti-Pattern guard). Author `test_extract_ops_result_ignores_supervisor_prose` and `test_extract_ops_result_tolerates_malformed_content_blocks` mirroring the proposal analogs (use `pytest.raises(RuntimeError)`). Author `test_full_supervisor_does_not_mutate_stage_scoped_supervisors`: assert `build_supervisor().tool_names` contains `gig_triage_agent` and NOT `proposal_contract_agent`/`ops_agent`, and `build_proposal_supervisor().tool_names` contains `proposal_contract_agent` and NOT `gig_triage_agent`/`ops_agent` (D-01). Only touch supervisor.py if a hardening test surfaces a real defect in the Task 1 extractor (e.g. a missing guard) — otherwise this task is test-only.
  </action>
  <verify>
    <automated>cd backend && python3 -m pytest tests/test_full_supervisor_wiring.py tests/test_proposal_supervisor_wiring.py tests/test_supervisor_wiring.py -q</automated>
    <fails_when>non-zero exit, or "failed"/"error" in the pytest summary line, or "0 passed"</fails_when>
    <automated>cd backend && python3 -m pytest</automated>
    <fails_when>non-zero exit, or "failed"/"error" in the final pytest summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - A test proves `extract_ops_result` returns the ops json even when a different specialist's json appears earlier in the trace (name disambiguation, D-01).
    - A test proves `extract_ops_result` raises `RuntimeError` (not `TypeError`) on malformed/absent traces.
    - A test proves `build_supervisor`/`build_proposal_supervisor` remain single-tool and were not mutated (D-01 prohibition).
    - Full suite green: `cd backend && python3 -m pytest`.
  </acceptance_criteria>
  <done>The by-name extractor is proven correct against the multi-tool first-match bug, malformed input, and re-authored prose; the existing stage-scoped supervisors are proven untouched; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FastAPI → build_full_supervisor (live path) | Only reached when `OPS_BACKEND=supervisor`; deterministic default never crosses it |
| build_full_supervisor → Bedrock | External model call (manual-only verification; no automated test crosses it) |
| supervisor.messages → extract_ops_result | Untrusted/possibly-malformed trace structure walked defensively |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-STORE | Tampering | backend/agents/ops_agent.py | medium | mitigate | ops_agent must not import the store; test_single_writer.py AST scan covers it automatically (Task 1 verify). |
| T-06-DISAMB | Tampering | extract_ops_result on a 3-tool trace | high | mitigate | By-name two-pass disambiguation (RESEARCH Pattern 5) instead of first-json-block scan; guarded by the multi-toolResult test (Task 2). A wrong-specialist result silently merged would corrupt the Engagement Record. |
| T-06-LEAK | Information Disclosure | live path exceptions | high | mitigate | The live path surfaces through Plan 06-01's ops `/advance` branch, which maps every exception via `map_bedrock_error` → credential-free 503 (no parallel handling added here). |
</threat_model>

<verification>
- Offline construction + extraction proof: `cd backend && python3 -m pytest tests/test_full_supervisor_wiring.py`.
- ORC-01/SC1 (four traceable agents): construction asserts three tools + four distinct Agent instances offline; the LIVE four-agent Bedrock trace is documented MANUAL verification (set real AWS creds + `OPS_BACKEND=supervisor`, run capture→proposal→ops, inspect the supervisor trace for four distinct Agent invocations) — same precedent as Phases 1/3/5 (D-08).
- D-01 prohibition: build_supervisor/build_proposal_supervisor unchanged (test_full_supervisor_wiring.py + existing test_proposal_supervisor_wiring.py green).
- Full suite regression: `cd backend && python3 -m pytest`.
</verification>

<success_criteria>
- ORC-01 provable offline: one Supervisor with all three specialist tools + four distinct Agent instances.
- The by-name extractor returns the correct specialist's result from a multi-tool trace and is malformed-input safe.
- No existing Phase 3/5 supervisor or extractor behavior changed; no new external package installed.
</success_criteria>

<output>
Create `.planning/phases/06-ops-agent-fixtures-full-supervisor-wiring/06-02-SUMMARY.md` when done.
</output>
