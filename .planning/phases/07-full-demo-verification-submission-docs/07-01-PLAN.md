---
phase: 07-full-demo-verification-submission-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/scripts/run_demo.py
  - backend/tests/test_demo_determinism.py
  - backend/tests/test_single_writer.py
autonomous: true
requirements: [DEMO-02]
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "One command — `cd backend && python3 -m scripts.run_demo --fixture creep` — drives capture -> proposal -> ops end-to-end in-process with no manual glue steps (DEMO-02/SC1)."
    - "Running the full fixture set (creep + clean) three times yields byte-identical decision fields, excluding engagement_id and invoice_flags[].days_overdue (DEMO-02/SC2)."
    - "run_demo.py reaches the Engagement Record store ONLY through TestClient(app) HTTP calls — it never imports the store, and the REC-03 single-writer guard now machine-enforces this for backend/scripts/ (T-07-STORE)."
  artifacts:
    - backend/scripts/run_demo.py
    - backend/tests/test_demo_determinism.py
  key_links:
    - "run_demo.py imports `app` from `api` and constructs `fastapi.testclient.TestClient(app)` — the exact in-process seam backend/tests/conftest.py's client fixture uses."
    - "backend/tests/test_single_writer.py SCAN_DIRS includes 'scripts' so the REC-03 import-graph guard now covers run_demo.py and every future script."
    - "test_demo_determinism.py excludes engagement_id (per-run UUID) and invoice_flags[].days_overdue (wall-clock derived) from the 3x equality, comparing only content-deterministic decision fields."
---

<objective>
Prove the complete Freelance Autopilot pipeline runs end-to-end from a single command and repeats deterministically — the DEMO-02 core capability — before any submission docs are written.

**MVP user story (faithful reframing of the ROADMAP Phase 7 Goal, which is outcome-shaped, not user-story-shaped):**
**As a** hackathon submitter, **I want to** run the full capture -> triage -> proposal/contract -> ops pipeline with one command and prove it produces identical results across repeated runs, **so that** a judge can reproduce the demo deterministically inside the 5-minute window without any manual glue steps.

Purpose: DEMO-02 is the load-bearing submission requirement — a non-deterministic demo fails judging. This plan ships the tracer entrypoint (`run_demo.py`) that the demo script (Plan 07-02) narrates over, plus the determinism proof that guards against a flaky recorded walkthrough.
Output: `backend/scripts/run_demo.py` (the demo entrypoint, D-01), `backend/tests/test_demo_determinism.py` (the 3x-identical proof, D-02), and a hardened `backend/tests/test_single_writer.py` that machine-enforces REC-03 for `backend/scripts/`.
</objective>

<execution_context>
@/home/user/Freelance-Autopilot/.claude/gsd-core/workflows/execute-plan.md
@/home/user/Freelance-Autopilot/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-full-demo-verification-submission-docs/07-CONTEXT.md
@.planning/phases/07-full-demo-verification-submission-docs/07-RESEARCH.md
@.planning/phases/07-full-demo-verification-submission-docs/07-PATTERNS.md

# Verified source of truth for the exact call sequence and DI seams this plan wraps:
@backend/api.py
@backend/tests/conftest.py
@backend/tests/test_advance_endpoint.py
@backend/tests/test_single_writer.py
@backend/fixtures/loader.py
</context>

## Artifacts this phase produces

The full Phase 7 artifact set (delivered across Plans 07-01 and 07-02):

| Artifact | Plan | Requirement |
|----------|------|-------------|
| `backend/scripts/run_demo.py` | 07-01 | DEMO-02 |
| `backend/tests/test_demo_determinism.py` | 07-01 | DEMO-02 |
| `backend/tests/test_submission_presence.py` | 07-02 | DEMO-03/04/05 |
| `README.md` (repo root) | 07-02 | DEMO-03 |
| `LICENSE` (repo root) | 07-02 | DEMO-04 |
| `docs/architecture.md` | 07-02 | DEMO-05 |
| `docs/demo-script.md` | 07-02 | DEMO-05 |

Label mapping follows the authoritative `.planning/REQUIREMENTS.md` text: DEMO-04 = OSI license, DEMO-05 = architecture diagram + demo script (CONTEXT.md's D-04/D-05 decision letters are offset by one; content is delivered in full either way).

<tasks>

<task type="tracer">
  <name>Task 1: TRACER — run_demo.py drives capture -> proposal -> ops end-to-end, one command (DEMO-02/SC1, D-01)</name>
  <files>backend/scripts/run_demo.py</files>
  <precondition>The integrated Phase 1/3/5/6 backend is present on this branch and `cd backend && python3 -m pytest -q` reports 118 passed before starting (the tracer wraps this existing pipeline; it adds no agent behavior).</precondition>
  <reversibility rating="costly">D-01 flags the entrypoint's CLI contract (module path + flags) as the interface the demo script in Plan 07-02 hard-codes. Changing the flags later means editing docs/demo-script.md and README.md too — pick the flag names deliberately now (`--fixture creep|clean`, `--ambiguous`) and keep them stable.</reversibility>
  <read_first>
    - backend/tests/conftest.py (the `client` fixture: `from api import app`; `with TestClient(app) as ...` — reuse this construction, but do NOT override get_store: run_demo uses the real default FileEngagementStore per D-01).
    - backend/tests/test_advance_endpoint.py::test_advance_ops_after_proposal_completes_both_stages (lines 135-176 — the exact verified capture -> proposal -> ops call sequence and the assertion that scope_creep_flags + invoice_flags == 2 for the creep fixture).
    - backend/tests/test_advance_endpoint.py::test_advance_ambiguous_scope_escalates_and_round_trips (lines 106-132 — the ambiguous job text `"Looking for someone to help with ongoing design work."` that deterministically yields needs_human_input=True with no contract).
    - backend/api.py (the three routes: `POST /capture` taking a JobSlice body {title, description, budget}; `POST /engagements/{engagement_id}/advance` with `stage` and `fixture=Literal["creep","clean"]` query params).
    - backend/fixtures/loader.py (public functions load_client_thread / load_payment_schedule / load_upwork_jobs — for reference only; the ops fixture is selected via the `fixture` query param, run_demo does NOT load fixtures directly).
    - backend/scripts/smoke_test_agents_as_tools.py (existing backend/scripts/ convention: a build/pure-construction function separated from a `main()` guarded by `if __name__ == "__main__": main()`, plus a module docstring stating what it does NOT do).
    - RESEARCH.md Pattern 1 + Pitfall 2 (the ONLY verified-working invocation is `cd backend && python3 -m scripts.run_demo`; the bare `python3 backend/scripts/run_demo.py` form raises ModuleNotFoundError: No module named 'api').
  </read_first>
  <action>
    Create backend/scripts/run_demo.py as a thin CLI driver over the UNMODIFIED FastAPI app, implementing D-01. Open with a module docstring citing DEMO-02/SC1/D-01 and stating the two things it does NOT do: it does not add a second store writer (REC-03 — it only talks to the app through TestClient) and it does not require AWS credentials by default (deterministic placeholder backends).

    Structure per the backend/scripts/ convention: a `build_client()` that returns a `TestClient(app)` (import `app` from `api`; do NOT call app.dependency_overrides — run_demo exercises the real default FileEngagementStore under backend/data/engagements/, which is gitignored, so the demo behaves like a real run per D-01/Pattern 1); a `run_pipeline(client, fixture, ambiguous)` that performs and prints the progression; and a `main()` using argparse with `--fixture` (choices creep/clean, default creep) selecting the ops variant, and `--ambiguous` (a flag) that swaps the capture job to the ambiguous scenario. Guard the run under `if __name__ == "__main__": main()`.

    run_pipeline steps, mirroring the verified sequence in the read_first tests: (1) POST /capture with the clear-scope job body {title: "Build a marketing site", description: "Standard React build with a clear scope, three deliverable phases, and a deadline in 6 weeks.", budget: 2000.0} for the normal path, OR the ambiguous body {title: "t", description: "Looking for someone to help with ongoing design work.", budget: 500.0} when --ambiguous is set; print the triage verdict, score, and reasoning from the response body. (2) POST /engagements/{engagement_id}/advance?stage=proposal; print the proposal text and either the contract text or, when needs_human_input is True, the escalation question. (3) If the proposal escalated (needs_human_input True, contract None), print that ops is skipped because no signed contract exists and stop before the ops stage — do not send the ops call; otherwise POST /engagements/{engagement_id}/advance?stage=ops&fixture={fixture} and print the ops scope_creep_flags, invoice_flags, and status_updates.

    Output format is Claude's discretion (favor human-readable narration for the recorded video), but the LAST line of stdout MUST be a stable machine-parseable summary line so the demo path is self-verifying: for a completed run print `SUMMARY fixture=<fixture> triage_verdict=<verdict> ops_cards=<N>` where N is len(scope_creep_flags)+len(invoice_flags); for the ambiguous run print `SUMMARY scenario=ambiguous triage_verdict=<verdict> proposal=needs_human_input`. Do NOT import the store or any store.* module anywhere in this file — REC-03/T-07-STORE.
  </action>
  <verify>
    <automated>cd backend && python3 -m scripts.run_demo --fixture creep</automated>
    <fails_when>non-zero exit; or "ModuleNotFoundError" / "Traceback" appears in output; or the final stdout line does not contain "ops_cards=2"</fails_when>
  </verify>
  <verify>
    <automated>cd backend && python3 -m scripts.run_demo --fixture clean</automated>
    <fails_when>non-zero exit; or the final stdout line does not contain "ops_cards=0"</fails_when>
  </verify>
  <verify>
    <automated>cd backend && python3 -m scripts.run_demo --ambiguous</automated>
    <fails_when>non-zero exit; or the final stdout line does not contain "proposal=needs_human_input"</fails_when>
  </verify>
  <acceptance_criteria>
    - DEMO-02/SC1: `cd backend && python3 -m scripts.run_demo --fixture creep` runs capture -> proposal -> ops in-process and exits 0 with a final summary line reporting ops_cards=2 (1 scope-creep + 1 overdue-invoice card).
    - `--fixture clean` produces ops_cards=0 (proving the checks are conditional, not hardcoded).
    - `--ambiguous` escalates at the proposal stage (proposal=needs_human_input) and skips the ops call because no contract exists.
    - run_demo.py contains no `import store` / `from store` statement (REC-03/T-07-STORE, verified structurally by Task 3).
  </acceptance_criteria>
  <done>run_demo.py exists, runs end-to-end for all three scenarios via the verified `python3 -m scripts.run_demo` invocation, and is committed.</done>
</task>

<task type="auto">
  <name>Task 2: Determinism proof — full fixture set 3x identical + pipeline populates all slices (DEMO-02/SC2 + SC1, D-02/D-07(a)(b))</name>
  <files>backend/tests/test_demo_determinism.py</files>
  <read_first>
    - RESEARCH.md Code Examples "Determinism test skeleton (D-02)" — the exact DECISION_FIELDS_EXCLUDING_VOLATILE helper and the creep/clean loop pattern; reuse it verbatim as the starting point.
    - RESEARCH.md Pitfall 1 — the full exclusion rationale: engagement_id is a per-run UUID (models/engagement_record.py:139) and invoice_flags[].days_overdue is derived from date.today() (backend/tools/check_invoice_status.py), so both MUST be excluded from the 3x equality; every other field (triage.verdict/score/reasoning, proposal.text/needs_human_input/question, contract.text/payment_schedule, ops.status_updates[].text, ops.scope_creep_flags, ops.invoice_flags[].milestone_label/due_date) is content-deterministic.
    - backend/tests/conftest.py — reuse the `client` fixture (tmp-path-isolated store) so this TEST never touches backend/data/engagements/ (unlike run_demo.py, which uses the real store by design).
    - backend/tests/test_advance_endpoint.py — the imports pattern (`from api import app`) and the capture/advance call shapes.
  </read_first>
  <action>
    Create backend/tests/test_demo_determinism.py with a module docstring citing DEMO-02/SC1/SC2/D-02/D-07(a)(b). Import the `client` fixture behavior from conftest (use the fixture as a test argument). Implement a curated decision-field extractor that takes an advance/get response body and returns a dict of ONLY the deterministic decision fields, dropping engagement_id entirely and stripping days_overdue from each invoice flag (keep milestone_label and due_date). Implement a `_run_once(client, fixture)` helper that runs the full sequence: POST /capture with the clear-scope job, POST advance?stage=proposal, POST advance?stage=ops&fixture={fixture}, then returns the curated dict from the ops response body.

    Test 1 (SC2): for each fixture in ("creep", "clean"), call _run_once three times and assert run[0] == run[1] == run[2] — one assertion per fixture, using a fresh engagement per run. Because engagement_id and days_overdue are excluded, the comparison is stable across runs and across a day boundary.

    Test 2 (SC1 end-to-end population): run the full capture -> proposal -> ops sequence once for the creep fixture and assert every stage slice is populated in the final GET body — triage is non-null with verdict in {"apply","skip"}, proposal.text is truthy, contract is non-null with a non-empty payment_schedule, and ops has a non-empty status_updates list. This proves the demo driver advances one record through all three stages with the record's slices populating (D-07(b)), independent of the equality axis.
  </action>
  <verify>
    <automated>cd backend && python3 -m pytest tests/test_demo_determinism.py -q</automated>
    <fails_when>non-zero exit, or "failed" / "0 passed" appears in the pytest summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - DEMO-02/SC2: running the creep and clean fixture sets three times each yields byte-identical curated decision dicts (engagement_id and invoice_flags[].days_overdue excluded).
    - DEMO-02/SC1: the end-to-end population test confirms triage, proposal, contract, and ops slices are all populated after one capture -> proposal -> ops run.
    - The test uses the conftest tmp-isolated `client` fixture and never writes to backend/data/engagements/.
  </acceptance_criteria>
  <done>test_demo_determinism.py passes offline in the existing suite and is committed.</done>
</task>

<task type="auto">
  <name>Task 3: Harden the REC-03 single-writer guard to cover backend/scripts/ (T-07-STORE machine enforcement)</name>
  <files>backend/tests/test_single_writer.py</files>
  <read_first>
    - backend/tests/test_single_writer.py (line 13: `SCAN_DIRS = ["agents", "tools", "fixtures"]`; the test walks each dir's *.py via ast and flags any import of `store`/`backend.store`). backend/scripts/ is currently NOT scanned (RESEARCH.md Pitfall 4).
    - Verified this session: no file under backend/scripts/ imports the store, so adding "scripts" to SCAN_DIRS surfaces no pre-existing violation.
  </read_first>
  <action>
    Add "scripts" to the SCAN_DIRS list in backend/tests/test_single_writer.py so the existing ast-based import-graph guard also walks backend/scripts/*.py, permanently machine-enforcing REC-03 for run_demo.py and every future script (the T-07-STORE mitigation flagged for this phase). Do not change the guard logic, the FORBIDDEN_MODULE_PREFIXES, or the repo_root resolution — only extend the scanned directory list. Update the test's module docstring to note scripts/ is now covered.
  </action>
  <verify>
    <automated>cd backend && python3 -m pytest tests/test_single_writer.py -q</automated>
    <fails_when>non-zero exit, or "failed" / "0 passed" appears in the pytest summary line (a non-zero exit here means a script imports the store — a real REC-03 violation)</fails_when>
  </verify>
  <verify>
    <automated>cd backend && grep -v '^#' tests/test_single_writer.py | grep -c '"scripts"'</automated>
    <fails_when>output is 0 (the "scripts" entry was not added to SCAN_DIRS)</fails_when>
  </verify>
  <acceptance_criteria>
    - SCAN_DIRS in test_single_writer.py includes "scripts"; the guard now walks backend/scripts/*.py.
    - The single-writer test passes, proving no module under agents/, tools/, fixtures/, or scripts/ imports the store (REC-03/T-07-STORE).
  </acceptance_criteria>
  <done>test_single_writer.py scans backend/scripts/, passes, and is committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| demo operator -> in-process app | run_demo.py drives the FastAPI app via TestClient in the same process; no network socket is opened, no external input surface is added. |
| script -> Engagement Record store | run_demo.py must reach persistence ONLY through the app's HTTP routes (api.py is the sole store writer, REC-03), never by importing the store. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-STORE | Tampering | backend/scripts/run_demo.py (unintended second store-writer path) | high | mitigate | Construct run_demo.py to call only `TestClient(app)` / the public HTTP surface, never `store.*` (Task 1); AND extend test_single_writer.py SCAN_DIRS to include "scripts" (Task 3) so the REC-03 import-graph guard machine-enforces this for run_demo.py and all future scripts. |
| T-07-DET | Tampering (false determinism) | backend/tests/test_demo_determinism.py | medium | mitigate | Exclude engagement_id and invoice_flags[].days_overdue from the 3x equality (Task 2) so the SC2 proof is stable across runs and day boundaries and cannot be defeated by a spurious pass/fail. |
| T-07-SC | Tampering | npm/pip/cargo installs | n/a | accept | No new external packages this phase (RESEARCH.md Package Legitimacy Audit: N/A); no install task exists, so no package-legitimacy checkpoint is required. |

No new authentication, session, access-control, input-validation, or cryptography surface is introduced (RESEARCH.md Security Domain: all ASVS categories "no"). ASVS L1; block on high — T-07-STORE is mitigated by construction and by an automated guard.
</threat_model>

<verification>
Full suite (existing 118 tests + the two new determinism tests, with the hardened single-writer guard) must be green:

`cd backend && python3 -m pytest -q` — expect all passed, 0 failed.

The tracer entrypoint must run for all three scenarios via the verified invocation (`cd backend && python3 -m scripts.run_demo --fixture creep|clean` and `--ambiguous`), each exiting 0 with the expected SUMMARY line.
</verification>

<success_criteria>
- DEMO-02/SC1: a single command runs capture -> proposal -> ops with no manual glue steps.
- DEMO-02/SC2: the full fixture set is provably identical across three runs (curated decision fields).
- REC-03/T-07-STORE: run_demo.py never imports the store, and the single-writer guard now enforces this for backend/scripts/.
- No new external packages installed; the suite stays offline and deterministic.
</success_criteria>

<output>
Create `.planning/phases/07-full-demo-verification-submission-docs/07-01-SUMMARY.md` when done.
</output>
